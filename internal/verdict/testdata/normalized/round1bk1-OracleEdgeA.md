{
  "verdict": "VERDICT: REJECT",
  "coverage": "Coverage: 22 families, 88 probes, 0 skipped — local://oracle-edge-A.md",
  "matrix": "local://oracle-edge-A.md",
  "scope": [
    {
      "id": "S1",
      "text": "during a DELETE, requests already in flight whose owner gets stopped crash with 500 (`unhandled_error ... callee exited ... Noproc` in `connection_owner.dispatch`). Race probe: head 13–19 of 400 GETs, base 4 of 400, same crash class. This is owner lifecycle in the registry, not the edge mapping."
    },
    {
      "id": "S2",
      "text": "a meta.db fault during API-key lookup (`api_keys` table renamed) returns 401 with no error line. Base behaves the same. It is a PER-6-style server-fault-as-401 on the key path; `meta_db.authenticate_api_key` classifies lookup failure as `KeyRejected`, which is wave 0's S3 code, not this slice's."
    }
  ],
  "reply": "# Oracle seat A (bug-hunt): slice/1bk1-edge @ a29e1a0, base 9908cfa\n\nThe product code works. I checked it against the base commit for every item below:\n- **D1:** every `ResolveError` variant, driven through `authenticate`, `authenticate_jwt_only`, the dev-mode path and the JWT path, returns its specified status. Each server fault logs exactly one `tenant_provision_failed` line. At base every one of these returned 401.\n- **D2:** the EDGE-2 race dropped from 28–32 hits per 100 runs at base to 0 per 100.\n- **D4:** EDGE-5 now fails closed on every verb I tried.\n- **Composition root:** the real server, booted with `gleam run`, behaves correctly in four config shapes.\n- **Legs:** I reproduced all criterion legs for D1, D2, D4 and D5.\n- **Full suite:** `694 passed, no failures`, both normally and with `ulimit -n 1024` set inside the dev shell.\n\nI am rejecting for two test-coverage regressions. Each is a mutant that a base test catches and no head test catches.\n\n## BLOCKING\n\n**B1: `server/test/woodshed_server/export_test.gleam:96-100` (`billing: None`). No test now checks that a lapsed account keeps export.**\n- FINDING: I added a scratch mutant in router.gleam that puts the export route behind the lapse gate: `case ctx.entitlements.session_writes { True -> api_export.get_export(..)  False -> write_gated() }`. I then ran `eunit:test([export_test, router_entitlements_test, router_billing_test])`.\n  - Head: `All 39 tests passed.`\n  - Base: `lapsed_tenant_keeps_export_but_loses_session_writes_test...*failed*  Failed: 1. Passed: 37.`\n  - `grep -rln 'account/export' server/test` finds only export_test.gleam, so no other test covers this.\n- EXPLANATION (inferred from code): with `billing: None`, `resolve_entitlements` takes the constant-full path. The tenant marked lapsed at export_test.gleam:249-275 is never lapsed on the request path, so the line-274 check (\"Export survives the lapse\") passes whether or not export is gated. The reply's preserve statement (\"all assertions still hold\") is true but hollow for this test.\n- The brief's text prescribes `billing: None` here. The fix below changes that prescription, so the orchestrator needs to rule on it.\n- REQUIRED FIX: build this fixture with a complete `web.Billing(secret_key:, webhook_secret:, price_id:, app_base_url:)`. That is a config-legal state, and it removes the illegal partial state just as `None` does.\n  - Checked in scratch: with that change, export_test is 4/4 green unmutated.\n  - Under the mutant above, only `lapsed_tenant_keeps_export...` goes red.\n\n**B2: `server/test/woodshed_server/meta_db_test.gleam:141-146` (the sha256 test was deleted, not moved). Nothing now checks the stored API-key hash format.**\n- FINDING: in scratch I dropped `|> string.lowercase` from `meta_db.sha256_hex` (meta_db.gleam:1474-1477). I then ran `eunit:test([meta_db_test, api_key_policy_test, router_auth_test, export_test])`.\n  - Head: `Failed: 0. Passed: 53.`\n  - Base: `sha256_hex_is_deterministic_and_lowercase_test...*failed*`\n- Two brief clauses are not met:\n  - The ownership line says \"move/keep their tests through `authenticate_api_key`\".\n  - D5 says \"`meta_db_test` covers the same behaviour\".\n  - The lowercase part of the stored hash format now has no test at all.\n- EXPLANATION (inferred): hashes already in production meta.db are lowercase hex. A format change would lock out every existing key, and the suite would stay green.\n- REQUIRED FIX: add a meta_db_test that inserts an `api_keys` row directly with the lowercase SHA-256 hex of a fixed plaintext (a literal), then asserts `authenticate_api_key(meta, plaintext)` returns `KeyAuthed` for that tenant.\n  - Checked in scratch: 23/23 green unmutated.\n  - Under the drop-lowercase mutant, only that test goes red.\n\n## Nits (do not gate)\n- **N1:** the reply's D4 leg-b explanation says `POST /api/flags answered 201`. The measured failure is `415 should equal 403`: the request has no body, so under the mutant it passes the gate and the handler rejects it. The leg itself reproduces.\n- **N2:** the reply's D3 base-hit count is 21. The D3 grep finds 22 lines, and the reply's list names test_services.gleam:47, which does not match that grep.\n- **N3:** the doc comment at web.gleam:380 still spells out the key format (`Authorization: Bearer gt_...`). The quoted-literal grep is 0, but EDGE-10's intent is that web no longer knows the prefix.\n- **N4:** web.gleam:753-756 throws away the cause carried by `ProvisionFailed(_)` / `StartFailed(_)`. The error line is `level=error msg=tenant_provision_failed tenant=dev` with no reason, while the sibling lines (`entitlements_unavailable`, `account_view_failed`) carry `error=`. The registry does not log the cause either. It still improves on base, which logged nothing.\n- **N5:** these scratch mutants pass all 10 web-facing test modules:\n  - `Deleting→401`, `Evicted→401` and `StartFailed→401` in `resolve_owner`. My probes show the behaviour is correct; no HTTP-level test covers those rows.\n  - `UnlinkFailed→204`. Same gap at base.\n  - `KeyRejected`/`KeyAuthed` falling through to the JWT branch in `authenticate_jwt_only`. The base analog is also green (97/97).\n  - The criterion's named mutants are all caught.\n- **N6:** the reply has no blast-radius statement (common contract item 4). The only out-of-range edits are the module docs at web.gleam:4-19 and entitlements.gleam:2-19 plus one import, all in owned files.\n- **N7:** router_auth_test's fixture now opens meta.db in every test and never stops it. FDs after 3× router_auth_test in one VM: head 27→474, base 27→318. The full suite still passes under a real 1024 limit. The handlers_test and hardening_test fixtures stop meta, but not under `defer`.\n- **N8 (design, seat B's call):** the argument for keeping `stripe_provider` on Services holds only in part. `clock` is used whether or not billing is on. `stripe_provider` is used only inside `Some(billing)` arms (account.gleam checkout and portal), and dev deployments wire `real_provider` without ever using it.\n- **N9 (method):** running `ulimit -n 1024` before `nix develop` does not take effect, because the dev shell resets the soft limit to 1048576 (I checked `/proc/self/limits`). Any 1024 claim must set the limit inside the shell. My 1024 runs did.\n\n## SCOPE (do not gate)\n- **S1:** during a DELETE, requests already in flight whose owner gets stopped crash with 500 (`unhandled_error ... callee exited ... Noproc` in `connection_owner.dispatch`). Race probe: head 13–19 of 400 GETs, base 4 of 400, same crash class. This is owner lifecycle in the registry, not the edge mapping.\n- **S2:** a meta.db fault during API-key lookup (`api_keys` table renamed) returns 401 with no error line. Base behaves the same. It is a PER-6-style server-fault-as-401 on the key path; `meta_db.authenticate_api_key` classifies lookup failure as `KeyRejected`, which is wave 0's S3 code, not this slice's.\n\n## Arbiter conditions\n- **Production callers:**\n  - `authenticate_api_key`: web.gleam:698 and web.gleam:802\n  - `delete_tenant`: api/account.gleam:72\n  - `retire_tenant`: api/account.gleam:76\n- **web.gleam greps:** `\"gt_\"` = 0, `sha256_hex` = 0, `lookup_api_key_by_hash` = 0.\n- **meta_db:** both functions are private.\n- **Ownership and seams:** all 16 files are in the ownership list (test_services_test.gleam by the architect's ruling). S4 and the revised S5 match the seam records exactly.\n\nScratch deleted; worktree list unchanged; reviewed worktree clean at a29e1a0. Model: anthropic/claude-opus-5-5 (@ORACLE).\n\nCoverage: 22 families, 88 probes, 0 skipped — local://oracle-edge-A.md\nVERDICT: REJECT"
}
