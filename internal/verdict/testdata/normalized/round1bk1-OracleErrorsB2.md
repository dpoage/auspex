{
  "verdict": "VERDICT: APPROVE",
  "coverage": "Coverage: 22 families, 83 probes, 2 skipped — local://oracle-errors-B-r1.md",
  "matrix": "local://oracle-errors-B-r1.md",
  "scope": [
    {
      "id": "S1",
      "text": "from_packs_error's new `call` parameter (service/today.gleam:231-250, a curried signature plus 5 call-site edits) feeds only the detail of the PackNotFound and PremiumRequired arms (:245, :247). Those arms never run: packs.gleam builds the variants only in set_enabled (:184, :188), and no from_packs_error call site reaches set_enabled. This need not exist, because it hardens an arm that never executes. It was ordered as a named nit, so the ruling is the orchestrator's."
    }
  ],
  "reply": "Seat B delta re-review of slice/1bk1-errors @ 6d70adf (fix-round base 7037166): acceptance replay first, then design review.\n\nIsolation: I exported both trees with `git archive` into /tmp/1bk1-errors-B-r1. First-party build dirs were deleted so every build was cold, and TMPDIR pointed inside the scratch dir. Every wire probe went through `test_services.request`, which is `router.handle_request`, and bracketed each request with markers so invariant lines are counted per request. Mutant legs ran as targeted eunit modules with gleeunit's `scale_timeouts 10`. Another seat (OracleEdgeB2) reported that the py kernel was shared and top-level names overwritten. My three kernel cells only parsed log files whose names exist only in my scratch dir, and each printed my own request markers, so the evidence is unaffected.\n\n**Blocker re-probes (base 7037166 → 6d70adf)**\n- **Disabled-pack POST (seat A B1):** 500 plus 1 invariant line → 404 {\"error\":\"drill not found\"} with 0 lines. A second hidden drill behaves the same. After re-enabling the pack the POST returns 201, and a repeat returns 409.\n- **Deleted onboarding row (my B1):** GET and PUT went from 404 {\"error\":\"not found\"} → 500 with exactly 1 `invariant_violated` line each. With the row present both answer 200.\n- **Ghost gate module (seat A B3):** 404 \"module not found\" → 500 plus 1 line. A client-supplied unknown module_id still answers 404 \"module not found\". The no-drift control answers 200 passed:true. After the 500 the transaction had rolled back: 0 answered rows and 0 ghost module_state rows.\n\n**Mutant legs**\n\n| Criterion | Mutant | Leg a (mutant + 7037166 tests) | Leg b (mutant + new test) | Leg c (6d70adf) |\n|---|---|---|---|---|\n| E4 | `raw_curriculum` → `active_curriculum` | 46 green | only the disabled-pack test red (500≠404) | 47 green |\n| E4 (implementer's form) | `compiled.drills` → `content.drills` | 46 green | same single test red | — |\n| E5 | `Store(NotFound)` | 7 green | only missing_seeded_row red (404≠500) | 8 green |\n| E6 | `ModuleNotFound` | 20 green | only the gate-header test red (404≠500) | 21 green |\n\n**E1 and E2 re-runs**\n- The ghost-drill incident still answers 500 plus 1 line.\n- Content drift still answers 500 plus 1 line: a composed drill deleted from content.db, and a drill's module_id changed to ghost-module.\n- E1 leg b re-run on the changed log_drill: only the E1 test goes red.\n- E1 leg c: 47 green. E2 leg c: 26 green.\n\n**F4, measured**\nThe only producers of today.Invariant are today.gleam:245 and :247 (from_packs_error), :1182 and :1195 (log_drill), and review.gleam:562.\n- The from_packs_error arms can't fire: PackNotFound and PremiumRequired are built only in set_enabled (packs.gleam:184, :188), which none of the five call sites reaches.\n- log_drill has one caller: router.gleam:159 → api/today.gleam:576.\n- review.gleam:562 can't fire: it searches the same `content` that rewind_proposal drew the module from.\n- For each of the six deleted arms I checked the wrapping service's today calls, plus curriculum's single library call. None reaches a live producer, so the deletions are correct.\n- The cold `--warnings-as-errors` build is green, format is clean, and module test counts changed only by the 3 new tests.\n\n**Radius:** I placed all 22 deleted-line hunks. The one excursion is the handlers invariant_error doc (nit N5). The from_packs_error signature change falls under the named nit.\n\n**Preserved assertions:** all 16 are ok by module. The test diff is pure additions.\n\n**Full suite:** one supervised `gleam test` at 6d70adf gave **693 passed, no failures** and exactly 4 invariant lines (gate ×1, onboarding ×2, ghost ×1).\n\n**Implementer claims:** the hash, numstat, four md5sums, leg counts, line counts, 693, 4 lines, the F4 table, the grep claim, and the quoted handlers doc all reproduced. The lapsed-premium claim does not hold at the wire (N1).\n\n**Records:** the 1bk.6.1 and guitartime-diy line numbers match 7037166, and both corrections (review provenance, library's second fetch) are accurate. Two small slips remain: N1 and N4.\n\n**Handlers doc vs behaviour:**\n- All 8 service_error_response copies delegate their own Store arm. All 4 services that have an Invariant variant delegate it.\n- A wrapped store error follows the endpoint's own mapping (assess and drills answer 500; curriculum, library and review go through store_error_response).\n- Seeded-singleton exclusion: I deleted each of settings, rotation, review_state and rewind_state in turn and hit 5 routes. Every result is identical on both builds, renamed by the today/review wrappers, with 0 invariant lines.\n- The singleton stores are imported only by today, review and onboarding. The doc's list omits rewind_state (N2).\n\n**Design review of the new variants**\n- DrillNotFound, onboarding.Invariant and assess.Invariant each have one reachable producer and a distinct arm. No caller defaults any of them away.\n- DrillNotFound matches the existing PUT /api/drills/:id/mastery behaviour for a pack-hidden drill: 404 \"drill not found\" on both builds.\n- log_drill's hidden-vs-absent split relies only on active ⊆ raw, which holds because the filters only remove entities. Raw content has 0 drill/module provenance mismatches out of 325 drills, so the second lookup cannot produce a false invariant under a pack toggle.\n- onboarding's wrap_store covers only get/set of the singleton.\n- The assess gate lookup has the latent coupling in N3. E6 does not need the pack branch.\n\nBLOCKING: none.\n\nNits: N1–N6 as listed. SCOPE: S1 (the from_packs_error `call` parameter only feeds dead arms). My previous SCOPE S1 is resolved.\n\nScratch /tmp/1bk1-errors-B-r1 is deleted and I created no worktrees. The reviewed worktree is clean at 6d70adf, and the worktree list is unchanged at 8.\n\nModel: anthropic/claude-opus-5-5 (harness role @ORACLE).\n\nCoverage: 22 families, 83 probes, 2 skipped — local://oracle-errors-B-r1.md\nVERDICT: APPROVE"
}
