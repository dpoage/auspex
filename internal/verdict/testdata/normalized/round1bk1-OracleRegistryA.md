{
  "verdict": "VERDICT: REJECT",
  "coverage": "Coverage: 14 families, 45 probes, 1 skipped — local://oracle-registry-A.md",
  "matrix": "local://oracle-registry-A.md",
  "scope": [
    {
      "id": "S1",
      "text": "Pre-existing class that needs a bead: connection_owner.stop (call_forever) on an already-dead Ready owner, with its Down still queued, panics the registry actor. HEAD: handle_delete:1037 (probe P9). Base: handle_evict:798 (same probe via evict). The sweep has the same call at :1367."
    }
  ],
  "reply": "Seat A (bug-hunt, concurrency) — slice W0-A registry @ 706e33f. Model: anthropic/claude-opus-5-5, harness role @ORACLE.\n\nIsolation: every probe ran in scratch copies under /tmp/1bk1-registry-A/, with git metadata detached. Base was built from `git archive 929d84e`. TMPDIR pointed into the scratch dir, and each run had its own erl VM in the dev shell (gleam 1.18.1, erlang 28). The probe source for both blockers is saved at local://oracle-registry-A-probes.gleam. I re-ran it against a fresh copy of the worktree at 706e33f and got the same outputs quoted below.\n\nBLOCKING\n\n1. tenant_registry.gleam:1040-1053, 548-562, 842-856, 1207-1232. If delete_tenant arrives while the tenant is Provisioning, the just-opened owner is handed out and never stopped. This breaks A2 (\"from the moment delete_tenant is called until it returns, no resolve(tenant_id) returns Ok(owner)\") and binding semantic (1) (\"the just-opened owner is closed, never handed out\").\n\nFINDING. Probe p1: provisioning of an existing tenant t is gated; delete_tenant is called; resolve B is sent after the Delete is parked; then the gate is released. The sys:get_state dump taken before B was sent shows t's Delete in parked_deletes.\n```\nPROBE P1 resolve A (sent BEFORE delete_tenant) reply: Ok(Ok(Owner(... <0.95.0>)))\nPROBE P1 resolve B (sent AFTER delete_tenant) reply: Ok(Ok(Owner(... <0.95.0>)))\nPROBE P1 resolve C (during retire) reply: Ok(Error(Deleting))\nPROBE P1 B-owner alive during retire: True\nPROBE P1 B-owner main file: \".../tenants/t.db\"\nPROBE P1 delete_tenant reply: Ok(Ok(Nil))\nPROBE P1 old gen main file exists after delete: Ok(False)\nPROBE P1 B-owner alive AFTER delete_tenant returned: True\nPROBE P1 rows visible through B-owner after its write (unlinked inode): 2\nPROBE P1 next resolve main file: \".../tenants/t.g1.db\"\nPROBE P1 next resolve row count: 0\n```\nThe write lands in the unlinked inode and is lost. The owner stays alive after delete_tenant returns and nothing ever stops it.\n\nState probe p1_state, taken after the settle with retire held: `tenants = [#(\"t\", DeletingTenant(#Ref, <0.93.0>, 2, [], [Subject(<0.91.0>)]))]`. The handed-out owner <0.92.0> is alive, and no Ready entry is left.\n\nIncident replay at the registry seam: 200 iterations, each running 4 concurrent resolves plus delete_tenant after 0-4ms of jitter. A HIT is a resolve-returned owner still alive after delete_tenant returned Ok.\n- HEAD, cached start (the verifier's shape): 0/200 hits, so that shape is fixed.\n- HEAD, uncached start: 32/200 hits.\n- Base (inline evict→bump→unlink): 20/200 cached, 26/200 uncached.\n\nBase forensics: at base, the same interleaving with evict in place of delete gives `BASE-P1 resolve A: Ok(Error(Evicted))`, `resolve B: Ok(Error(Evicted))`, `just-opened owner alive: False`.\n\nCoverage: the mutant CAS makes delete_tenant on a Provisioning tenant reply Ok(Nil) at once, with no retire and no unlink. Under it, woodshed_server@tenant_registry_test reports `All 29 tests passed`. The parked-delete path is not tested.\n\nEXPLANATION (inferred from the code and the state dump, not probed step by step):\n- handle_delete's Provisioning arm only records the delete in parked_deletes. It does not join pending_evicts.\n- handle_resolve's Provisioning arm keeps parking new resolves as waiters.\n- The `[], Ok(owner)` settle arm replies Ok(owner) to every waiter and installs Ready.\n- continue_after_settle then overwrites that Ready entry with DeletingTenant without calling connection_owner.stop.\n\nREQUIRED FIX: while a delete is parked on a Provisioning attempt, refuse resolves with Error(Deleting). Settle the attempt the way a parked evict does: stop the just-opened owner and never hand it out. Only then start the delete worker. Add a test that starts delete_tenant from a Provisioning tenant and fails under both the current code and CAS.\n\n2. tenant_registry.gleam:767-771. A late ProvisionDone(Ok(owner)) that lands on a DeletingTenant slot is dropped without closing the owner. The arm's comment says \"It carries no Ok owner (a delete worker never produces one)\". That is false: the message comes from a timed-out provisioner, not from the delete worker.\n\nFINDING. Probe p2 sets provision_timeout_ms to 150. The resolve times out, delete_tenant runs with retire held, and then the wedged open is released.\n```\nPROBE P2 resolve A after deadline: Ok(Error(ProvisioningTimedOut))\nPROBE P2 late owner alive 500ms after its ProvisionDone landed on Deleting: True\nPROBE P2 delete reply: Ok(Ok(Nil))\nPROBE P2 late owner alive after delete returned (+300ms): True\nPROBE P2 old gen main file exists after delete: Ok(False)\nPROBE P2 late owner main file: \".../tenants/t.db\"\nPROBE P2 late owner still reads deleted tenant rows: 1\n```\nControl p2_control, where the late ProvisionDone lands on an empty slot: `late owner alive 500ms after ProvisionDone (empty slot): False`. At base, all such arms close the owner, and base's late_provisioning_completion test covers that. Fix-mutant: adding `close_leaked_owner(result)` to the arm at :771 turns P2 into `late owner alive ...: False`.\n\nImpact: a leaked SQLite handle holds the deleted tenant's data in an unlinked inode until the process restarts. Reachability is narrow: it takes an open slower than provision_timeout_ms (30_000 at woodshed_server.gleam:72) followed by a delete before that open finishes.\n\nREQUIRED FIX: call close_leaked_owner(result) in that arm, correct the comment, and add a test for this interleaving.\n\nCRITERIA (all legs reproduced, invoked as the brief says)\n- A0: the grep count is 0 at HEAD and 8 at base. Six parallel runs were green (`All 29 tests passed` ×6) with a truly shared TMPDIR (`nix develop -c env TMPDIR=X erl ...`), plus 6 more green with a per-shell TMPDIR. I skipped re-running base, because its hard-coded /tmp paths would collide with sibling seats; the discriminator is the one cited from the premortem.\n- A1: leg a All 27; leg b Failed 2 (health_resets…, open_faults_split…); leg c All 29. A sub-mutant that drops only the `[], Error` InvalidTenantId exemption gives Failed 2.\n- A2: leg a All 28; leg b Failed 1 (resolve_during_delete_window…); leg c All 29.\n- A3: leg a All 28; leg b Failed 1 (delete_tenant_failure_ordering…); leg c All 29.\n- A4: leg a All 13; leg b Failed 1 (remove_files_fails_when_the_main_file_survives); leg c All 15.\n- Extra X mutant: leg a All 28; leg b test timed out; leg c All 29.\n- Full suite in the HEAD copy: `676 passed, no failures`.\n- Build: purged first-party caches, then `--warnings-as-errors`, with 0 warnings.\n- Ownership and blast radius: 4 files, all owned. Every deleted line falls inside the named radii.\n- Seam S1: all declarations match verbatim, and Config is identical to base.\n\nNITS (non-gating)\n- The parked-delete order contradicts its docs. State doc :327-332 and the comment at :1041 say the first request's retire runs. The code prepends (:1044-1045) and takes the head (:1207). Probe P3: `retire that ran: \"SECOND (delete2) retire\"`, and both callers got its reply.\n- delete_tenant(\"../x\", Ok-retire) runs retire for \"../x\" and then maps RemoveInvalidTenantId to RetireFailed (:1145). The doc at :175-177 says RetireFailed means \"files and generation untouched\", which is false here. Probe P5: `Error(RetireFailed)`, `retire ran ... for: Ok(\"../x\")`.\n- The delete worker has no deadline. With retire wedged for more than 2s: delete gets no reply, resolve returns `Error(Deleting)`, evict and a second delete get no reply within 1s, and health stays `Healthy`. Other tenants are fine. The brief does not require a deadline; any deadline would have to keep refusing resolves until the worker settles.\n- The implementer's reply is inaccurate in two places:\n  - A0 claims \"the two runs genuinely shared one namespace\". In fact each `TMPDIR=X nix develop` gets its own X/nix-shell.* TMPDIR. The criterion still holds under a truly shared TMPDIR.\n  - Extra X leg b says \"the rest of the module stayed green\". The log says `Passed: 25`, `One or more tests were cancelled`.\n- remove_files_fails_when_the_main_file_survives_test leaves a 0o500 tenants dir behind when it fails. Under the A4 mutant, `rm` of its temp dir hit `Permission denied`.\n\nSCOPE\n- Pre-existing defect class, which needs a bead (I made no bd writes, per my constraints). connection_owner.stop is call_forever, so calling it on a Ready owner that has already died, while its Down is still queued, panics the registry actor.\n  - HEAD, probe P9 (sys:suspend, Delete, kill the owner, resume): registry alive False, crash at handle_delete:1037.\n  - Base, the same sequence via evict: crash at handle_evict:798.\n  - The sweep has the same call at :1367.\n  - The diff adds one more site of this class, which the brief mandates.\n\nNULL RESULTS\n- Health accounting across Deleting is correct: with the counter at 2, five refusals leave it Healthy, a crashed delete leaves it Healthy, and a parked delete behind GenerationUnavailable gives Unhealthy(3).\n- A sweep tick during Deleting leaves the slot alone.\n- Evict and a second delete parked behind a retire that panics both get replies, and the rows survive.\n- Stale DOWN messages and stale-tag DeleteDone messages are dropped. The injection format is valid: the current tag is accepted.\n- The 40×40 random-op stress run: every call returned, the registry stayed alive, and each tenant resolved on its current generation afterwards.\n\nCleanup: /tmp/1bk1-registry-A is removed. I created no worktrees or branches, and the reviewed worktree is clean at 706e33f. My nix develop calls made without a TMPDIR left empty /tmp/nix-shell.* dirs. They cannot be told apart from siblings' identical dirs, so I did not delete them.\n\nCoverage: 14 families, 45 probes, 1 skipped — local://oracle-registry-A.md\nVERDICT: REJECT"
}
