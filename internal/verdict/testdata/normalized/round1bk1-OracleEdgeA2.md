{
  "verdict": "VERDICT: APPROVE",
  "coverage": "Coverage: 33 families, 70 probes, 0 skipped — local://oracle-edge-A-r1.md",
  "matrix": "local://oracle-edge-A-r1.md",
  "scope": [
    {
      "id": "S1",
      "text": "Answers 500 with no error line when meta.db faults, for both JWT and dev callers; a29e1a0 behaves the same. Outside the fix diff."
    }
  ],
  "reply": "# Oracle seat A (bug-hunt): delta re-review of slice/1bk1-edge @ e364cc6 (fix round 1, from a29e1a0)\n\nBoth round-0 blockers are fixed, and all three legs of D6, D7 and D8 reproduce. When the API-key lookup fails, every route that checks the key now answers 500 with exactly one `api_key_lookup_failed` line, in dev mode and JWT mode, and never falls back to the dev tenant. At a29e1a0 the same faults answered 401 with no log line. Full `gleam test`, supervised: `697 passed, no failures`, EXIT=0.\n\n## Blocker re-probes and criterion legs\n- **B1 / D6.** Mutant A (router gates export on session_writes):\n  - leg a, new test disabled: export 4/4, router_entitlements 4/4, router_billing 31/31 green;\n  - leg b: `lapsed_tenant_keeps_export_test...*failed*  \"403 should equal 200\"  Failed: 1. Passed: 4.`\n  - Mutant B (`bool.guard(!session_writes, 402)` in get_export): leg a green; leg b `\"402 should equal 200\"`, Failed 1.\n  - Leg c: 5, 4 and 31 tests, all green.\n- **B2 / D7.** Mutant drops `string.lowercase`:\n  - leg a: meta_db 22, api_key_policy 9, router_auth 19, export 5, all green;\n  - leg b: `a_lowercase_sha256_hex_row_authenticates_test...*failed*  value => key_rejected`, Failed 1;\n  - `printf %s gt_stored-hash-format-pin | sha256sum` matches the test's literal digest.\n- **D8.** Mutant maps the fault back (`KeyDatabaseFailed(_) -> KeyRejected`):\n  - leg a: 22/18/9/5 green;\n  - leg b: the meta_db unit test fails with `value => key_rejected`, and router_auth fails with `\"401 should equal 500\"`;\n  - leg c: 23/19/9/5.\n  - Log line in the full-suite stdout (two occurrences): `level=error msg=api_key_lookup_failed error=\"SqlightError(GenericError, \\\"no such table: api_keys\\\", -1)\"`.\n- **D8, JWT-only path.** With `api_keys` renamed, dev mode, and a minted key on each of GET/POST /api/account/keys, DELETE /api/account/keys/1, DELETE /api/account, checkout and portal:\n  - every request answers 500 `{\"error\":\"internal error\"}` with one lookup line and `tenant=-`;\n  - the dev generation stays 0 and the tenant files are unchanged;\n  - a29e1a0 answered 401 to all of these, with no line.\n- **D8 replayed on a real `gleam run` server.** Same results: 500 on each of 4 routes with one line each, dev.db intact, `tenant_generations` empty, and a no-credential request answers 200.\n- **F4.** `pub fn bump_generation|purge_tenant` count: 0 (was 2).\n  - Five retire/purge mutants (the purge drops the generation row, the latest trial start wins, an absent start erases the tombstone, the purge skips keys, the purge deletes every tenant's keys) are each caught at e364cc6 as they were at a29e1a0.\n  - The deleted `purge_tenant_leaves_the_generation_row_test` is covered by other tests: under the drop-generation and skip-keys mutants, `retire_tenant_bumps_then_purges_and_preserves_trial_start_test` fails.\n- **F5.** Greps for the web mirror all return 0. D5 mutant (`KeyRejected -> resolve_jwt`): both dev-mode 401 tests fail with `\"200 should equal 401\"`; api_key_policy 9/9 and meta_db 23/23 stay green.\n- **Re-run legs on touched code.**\n  - D1 leg b (`ProvisionFailed -> 401`): `\"401 should equal 500\"`.\n  - D4 leg b now fails with `\"201 should equal 403\"`, so round-0 nit N1 is fixed.\n  - D1-D5 leg c: router_auth 19, handlers 2, tenant_generation 5, tenant_capacity 4, router_billing 31, export 5, api_key_policy 9, hardening 23, meta_db 23, test_services 3, router_entitlements 4.\n- **Build.** Fresh `gleam build --warnings-as-errors` compiles with 0 warnings; `gleam format --check` on the 6 touched files exits 0.\n\n## Radius\nI placed every deleted line. The out-of-site lines the implementer listed are all acceptable:\n- meta_db.gleam −25, −311, −443, −1041, −1226: each names `purge_tenant` or `bump_generation`, which F4 deletes; doc-only changes.\n- web.gleam −445..446: the new comment is accurate. `KeyIdentity(` is built only at web.gleam:774.\n- web.gleam −526..528, −548, −561, −563: these carry the F3 arm and the N4 cause.\n- web.gleam −385 and −391: same doc block as :380.\n- export_test.gleam −12..14: module-doc sentences the rename made stale.\n- The deleted meta_db_test test is covered by other tests, as above.\n\n## Families on the fix diff\n- **Fault injection** (full table in the matrix):\n  - duplicate hash rows answer 500 with the line \"returned multiple rows\"; an undecodable scope answers 500 with \"Decoder failed\"; a29e1a0 answered 401 to both;\n  - a stopped meta owner is unchanged: 500 crash;\n  - key-write dispatch is unchanged: an audit-insert fault gives 201 plus one line, a budget-read fault gives 500 plus one line;\n  - a lookup fault on a key write answers 500 plus one line, with no write and no audit row;\n  - no log line contains token material.\n- **`defer` teardown under a failing body.** A failing assertion or a panic propagates the original error, the temp dir is deleted, and the meta and content owners are released (+7 fds, the same as a passing body). a29e1a0 left the dir behind (+11).\n- **FDs after 3× router_auth_test in one VM.** e364cc6 28→82→136→190; a29e1a0 28→177→326→475; base 28→125→222→319. Round-0 nit N7 is fixed.\n- **Round-0 probe module** (resolve matrix, ordering, fixtures, DELETE, EDGE-5) re-run in this session on a29e1a0 and e364cc6: 161 of 162 outcome rows are identical. The one difference is del_concurrent ordering, which is 10/10 identical on both when repeated.\n- **Flake check.** The 5 new or rewritten tests pass 24/24 each.\n\n## BLOCKING\nNone.\n\n## Nits (do not gate)\n- **N1: web.gleam:720 and router_auth_test.gleam:239-245. The D8 test's JWT-only leg cannot detect a fall-through.**\n  - FINDING: I applied the mutant `meta_db.KeyLookupFailed(_) -> resolve_jwt(req, services)` in `authenticate_jwt_only`. All 18 web-facing modules stay green (261/261).\n  - Under that mutant, in dev mode with `api_keys` renamed:\n    - DELETE /api/account with the faulted key answers 204 and retires the dev tenant: dev.db is replaced by dev.g1.db and the generation goes 0→1;\n    - GET /api/account/keys answers the same 500 `{\"error\":\"internal error\"}` as correct code, with no lookup line.\n  - EXPLANATION (inferred from the probe): `list_keys` fails on the same renamed table, so status and body look the same either way. The test's note \"(which would answer 200)\" is true only for the /api/today leg.\n  - D8's named mutant is caught, and the code is correct today (matrix rows 12, 15, 26).\n  - Cheap fix: point the JWT-only leg at DELETE /api/account and assert `meta_db.generation(meta, \"dev\") == Ok(0)`.\n  - The orchestrator may upgrade this.\n- **N2:** `provisioning_timeout_answers_503_tenant_unavailable_test` leaves its temp dir behind. Provisioning is still running after the deferred delete and recreates tenants/dev.db.\n  - 2 leftover dirs per 3 module runs at both e364cc6 and a29e1a0; 0 at base.\n  - The registry is never stopped, which also explains the remaining +54 to +61 fds per run.\n- **N3:** the `KeyRejected` doc (\"never minted, or revoked\") misses a third case: a key purged by `retire_tenant`.\n- **N4:** tenant_db.gleam:113 still cites `meta_db.bump_generation`. The implementer disclosed this, and the fix list leaves it to the polish pass.\n\n## SCOPE (do not gate)\n- **S1:** `api/account.list_keys` answers 500 with no error line when meta.db faults, for both JWT and dev callers. a29e1a0 behaves the same. It is outside the fix diff.\n- Round-0 S2 is resolved by D8. Round-0 S1 is unchanged and was not re-probed.\n\nScratch /tmp/1bk1-edge-A-r1 is deleted, along with the one nix temp pair I created. The worktree is clean at e364cc6 and the worktree list is unchanged. The verdict was also sent to Round1bk1ArchitectC. Model: anthropic/claude-opus-5-5.\n\nCoverage: 33 families, 70 probes, 0 skipped — local://oracle-edge-A-r1.md\nVERDICT: APPROVE"
}
