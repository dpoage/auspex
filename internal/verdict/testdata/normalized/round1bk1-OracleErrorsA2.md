{
  "verdict": "VERDICT: APPROVE",
  "coverage": "Coverage: 14 families, 53 probes, 1 skipped — local://oracle-errors-A-r1.md",
  "matrix": "local://oracle-errors-A-r1.md",
  "scope": [
    {
      "id": "S1",
      "text": "the from_packs_error PackNotFound/PremiumRequired arms (today.gleam:245, :247) and review.gleam:562 build today.Invariant values that no reachable input produces; review:562 would now answer 500 with no log through Today(_)."
    }
  ],
  "reply": "# Seat A (bug-hunt): errors delta re-review at 6d70adf (previous commit 7037166)\n\n**Verdict: APPROVE.** All three of my earlier blockers are fixed at 6d70adf. The E4, E5 and E6 legs reproduce with their named mutants. The F4 deletions hold up when measured. The 16 preserved assertions pass. The full suite passes: `693 passed, no failures`.\n\n## Blockers re-probed\nEach probe ran through `router.handle_request` via `test_services.with_dev`, in its own `erl` process so its log lines could be counted on their own.\n\n- **B1 (disabled pack).**\n  - Setup: the real composer offered 4 pack drills. `PUT /api/packs/country {enabled:false}` returned 200.\n  - `POST .../drills` for `tq-country-boom-chick-chord-map`: 404 `{\"error\":\"drill not found\"}` with 0 `invariant_violated` lines. Repeating it gives 404 again.\n  - After re-enabling the pack, the same POST returns 201.\n  - `GET /api/sessions/open` while the pack is disabled: 409, unchanged.\n  - Control: disabling the unrelated `blues` pack and then logging the country drill returns 201.\n- **B2 (onboarding row deleted).**\n  - After `delete from onboarding`, `GET /api/onboarding` and `PUT /api/onboarding` each answer 500 `{\"error\":\"internal error\"}` with exactly 1 line: `detail=\"onboarding singleton row (id = 1) is missing\"`.\n  - Control with the row present: 200 and 200.\n- **B3 (gate header module missing).**\n  - After a drift update, `POST /api/assessments/checkpoint` answers 500 with exactly 1 line: `gate checkpoint 1 names module \\\"ghost-gate-module\\\" absent from the compiled content`.\n  - Nothing is persisted: 0 answered rows and 0 `module_state` rows for the ghost module. A retry gives 500 again, not 409.\n  - The client-supplied `GET ...?kind=gate&module_id=ghost-gate-module` still answers 404 `{\"error\":\"module not found\"}`.\n  - Control without drift: 200 `passed:true`.\n\n## Criterion legs (scratch clone at 6d70adf)\nLeg a runs the mutant against the 7037166 test file, leg b the mutant against the new test file, leg c has no mutant.\n\n| Criterion | Mutant | Leg a | Leg b | Leg c |\n|---|---|---|---|---|\n| E4 | today.gleam:1179 `compiled.drills` → `content.drills` | All 46 passed | Failed: 1, Passed: 46 — `log_drill_of_a_drill_hidden_by_a_disabled_pack_returns_404_test`, `500 should equal 404`, stdout shows the false invariant line | All 47 passed |\n| E5 | onboarding.gleam:30 → `Error(Store(types.NotFound))` | All 7 passed | Failed: 1, Passed: 7 — `missing_seeded_row_is_a_500_not_a_404_test`, `404 should equal 500` | All 8 passed |\n| E6 | assess.gleam:894-900 → `replace_error(ModuleNotFound)` | All 20 passed | Failed: 1, Passed: 20 — `gate_header_module_missing_from_content_returns_500_test`, `404 should equal 500` | All 21 passed |\n\n- **E1 leg c:** `api@today_test` passes 47/47, including `log_drill_of_a_drill_removed_from_content_returns_500_test`.\n- **E2 leg c:** `service@today_test` passes 26/26, including `advance_module_after_mastery_unknown_module_id_is_a_no_op_test`.\n- **Ghost-drill incident:** 500 plus exactly 1 line.\n\n## F4, measured\n- **Build:** a cold `gleam build --warnings-as-errors` (first-party caches purged) exits 0.\n- **Producers of `today.Invariant`:**\n  - today.gleam:245/247 (`from_packs_error`). `PackNotFound` and `PremiumRequired` are only built at packs.gleam:184/188, inside `set_enabled`, whose only caller is api/packs:82.\n  - today.gleam:1182/1195 (`log_drill`). Only reached from api/today:576, via router:159.\n  - review.gleam:562. Its module id is `candidate.module.id` (rewind.gleam:59), taken from the same content it searches.\n- **What the wrapping services call:** `grep -oE 'today_service\\.[A-Za-z_]+'` over assess, curriculum, drill_mastery, library, review and progression shows none of them calls `log_drill`. None of the six deleted arms has a reachable producer.\n- **Catch-all mutant:** I made `today.curriculum` and `curriculum_in_conn` return `Invariant(\"F4PROBE\")`. `GET /api/modules/fb-note-names`, the gate GET, the drills mastery PUT, `GET /api/notes` and `GET /api/review` all answered 500 `{\"error\":\"internal error\"}` with 0 log lines. The `Today(_)` catch-alls keep the 500.\n- **Imports:** putting `import ...service/today as today_service` back into api/assess or api/drills fails the build with \"Unused imported module\". Both import deletions are forced.\n\n## Families on the fix diff\n- **Drill missing from both the active and the compiled content, with country disabled:** 500 plus 1 line.\n- **Seeded (not composed) blues drill row with blues disabled:** 404 `drill not found`, 0 lines. After re-enabling: 201.\n- **Premium-hidden drill:**\n  - Service level: `log_drill(premium_entitled: False)` on a seeded `tq-classical-arpeggio-ladder` row returns `Error(DrillNotFound)` with 0 lines. With `True` it returns Ok.\n  - Not reachable on the wire: entitlements.gleam only has all-true and all-false constants, and in the lapsed state `session_writes: False` blocks the POST before the service runs.\n- **Pack-provided gate module:**\n  - `module_predicate` over the raw compiled content finds 0 of 34 Pack modules gate-eligible. Only `Primary` modules are eligible.\n  - `GET ...?kind=gate` on all 34 pack modules returns 422 every time.\n  - `grep -rl 'tool: quiz' curriculum/packs`, the `tool:\\s*quiz` variant, and a plain `grep -rl quiz` all return exit 1.\n  - So E6 does not need a pack-hidden branch today. It would only matter if a pack starts shipping quiz drills.\n- **Second lookup at today.gleam:1193:** all 325 compiled drills share their module's provenance (0 mismatches, 0 missing modules), so no pack toggle can hit it.\n- **Onboarding store:** only `get` and `set` on the singleton exist, and `NotFound` comes only from an empty result (lines 36 and 62). No client-named miss gets reclassified.\n\n## Preserved assertions and the full suite\n- **16 preserved assertions**, matched by name at 6d70adf: assess 2, curriculum 3, drills 1, library 4 (including the vault 500), review 1, today 5. All pass.\n- **Module totals:** assess 21, curriculum 20, drills 5, library 18, review 18, today 47, onboarding 8. Compared with 7037166, each count rose by exactly 1 only in the modules that gained the E4, E5 and E6 tests.\n- **Full `gleam test`:** one run in the head clone, 609 s: `693 passed, no failures`, EXIT=0. It printed 4 `invariant_violated` lines: gate ×1, onboarding ×2, ghost-removed-drill ×1.\n\n## BLOCKING\nNone.\n\n## Nits\n- **N1 (radius).** Every deleted line fits the fix list's radius except handlers.gleam:82-83, which falls outside the literal :26-69. It is a doc-only change, and F4 made the old text false; the implementer declared it. The `from_packs_error` signature change and its four call sites (today:211, :222, :225-237, :479, :1438) are the named nit. On the addition side, the `assess.Invariant` declaration at service/assess.gleam:59-63 is outside the literal :875-890, but F3 needs it.\n- **N2 (doc).** The packs.gleam:99-102 `raw_curriculum` doc still lists only the packs toggle UI as a use. `log_drill`'s miss path now uses it too. The file is outside this round's radius.\n- **N3 (carried coverage gap).** No test pins the \"exactly one / zero `invariant_violated` lines\" part: the E4 test asserts the 404 body, not stdout. I measured the log lines by hand instead (matrix row 48).\n\n## SCOPE\n- **S1.** The `PackNotFound`/`PremiumRequired` arms of `from_packs_error` (today.gleam:245, :247) and the construction at review.gleam:562 still build a `today.Invariant` that no reachable input produces (matrix row 36). Since F4, review.gleam:562 would answer 500 with no log line through `Today(_)`. It never runs.\n\n## Isolation and cleanup\n- All probes ran in git clones under `/tmp/1bk1-errors-A-r1`. Dependency caches were copied from the worktree with first-party artefacts and lock files purged. Every `nix develop` ran with `TMPDIR` inside the scratch dir.\n- I deleted the scratch dir afterwards. The reviewed worktree is clean at 6d70adf, and I created no worktrees (still 8).\n- Compared with the /tmp listing I took at the start, three empty `nix-develop-*` and three empty `nix-shell.*` directories are new (03:00–03:16), and `1bk1-edge-B-r1` has been removed. I believe none of these are mine, since all my nix runs pointed `TMPDIR` into my scratch dir, so I left them alone.\n- No bd writes. I did not contact the implementer.\n\nCoverage: 14 families, 53 probes, 1 skipped — local://oracle-errors-A-r1.md\n\nModel: anthropic/claude-opus-5-5 (the model in the harness workstation block; harness role @ORACLE)\n\nVERDICT: APPROVE"
}
