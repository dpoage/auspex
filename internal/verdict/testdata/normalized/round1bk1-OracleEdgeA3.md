{
  "verdict": "VERDICT: APPROVE",
  "coverage": "Coverage: 19 families, 32 probes, 0 skipped — local://oracle-edge-A-r2.md",
  "matrix": "local://oracle-edge-A-r2.md",
  "scope": [
    {
      "id": "S1",
      "text": "api/account.list_keys answers 500 with no error line on a meta.db fault. Outside the diff."
    }
  ],
  "reply": "# Oracle seat A (bug-hunt): delta re-review of slice/1bk1-edge @ 2e5b025 (fix round 2, from e364cc6)\n\nThe new test catches the fall-through mutant I reported as nit N1, so N1 is resolved. The fix changes only the test file, and the product code is byte-identical to e364cc6. I found no new defects.\n\n## Scope\n- `git diff --numstat e364cc6..2e5b025` lists one file: `25 5 server/test/woodshed_server/router_auth_test.gleam`.\n- The md5 of `git diff 929d84e..2e5b025 -- server/src` and of `git diff 929d84e..e364cc6 -- server/src` is the same: `706417ff50f337e79a2a7fd4a357804c`.\n- The deleted lines are e364cc6 :298-301 (doc comment) and :326 (code comment), both inside the allowed :295-333.\n\n## D8b: the JWT-only path (web.gleam:720 fall-through mutant)\nMutant: `meta_db.KeyLookupFailed(_) -> resolve_jwt(req, services)`.\n- **Leg a** (test file at e364cc6): router_auth `All 19 tests passed.` This reproduces the gap I reported as N1.\n- **Leg b** (test file at 2e5b025): the D8 test fails:\n  - `dev_mode_key_lookup_fault_is_500_not_401_test...*failed*` with `message => <<\"\\n204\\nshould equal\\n500\">>`\n  - `Failed: 1.  Skipped: 0.  Passed: 18.`\n  - captured log: `method=DELETE path=/api/account status=204 ... tenant=dev`\n- **Leg c** (2e5b025 unmutated, module run solo): `All 19 tests passed.`\n- **Is the generation assert live on its own?** With the mutant and the DELETE status and body asserts removed, the test fails with `\"Ok(1) should equal Ok(0)\"`.\n- **Is the keys leg a discriminator?** With the mutant and every DELETE-leg assert removed, the module is 19/19 green. The new comment says the keys leg is not a discriminator, and that is accurate.\n\n## Other mutants and criterion legs\n- **D8a** (`KeyDatabaseFailed(_) -> KeyRejected`): router_auth fails with `\"401 should equal 500\"`, meta_db fails with `value => key_rejected`, api_key_policy 9/9 green.\n- **M2** (resolve_context falls through to JWT): `\"200 should equal 500\"`.\n- **M3** (JWT-only answers 401): `\"401 should equal 500\"`, now caught by the DELETE leg.\n- **M4** (drop the log line): 19/19 green. The log line is checked from stdout by design, not by the test.\n- **D5 leg a** (D5 tests disabled): 17/17 green. **Leg b:** both D5 tests fail with `\"200 should equal 401\"`; api_key_policy 9/9 and meta_db 23/23 stay green.\n- **D1 leg b:** `\"401 should equal 500\"`.\n- The doc comment at :303-308 makes one claim per leg, and every claim matched the observed behavior (401, 200, and 204 with generation 0 to 1).\n\n## Fault injection on the new DELETE leg\nSetup: dev mode, `api_keys` renamed, key-bearing `DELETE /api/account`.\n- **Correct code:** answers 500 `{\"error\":\"internal error\"}`.\n  - The `tenants/dev.db`, `-shm` and `-wal` file sizes are identical before and after, whether or not the tenant was provisioned first.\n  - `tenant_generations` stays empty, the generation is `Ok(0)`, the key row survives and no audit row is written.\n  - After the table is restored, a request with no credential answers 200 as `dev`, and the key answers 200 as `key-owner`.\n- **Under the mutant:** answers 204, the `tenants/dev.db*` files are removed, and `tenant_generations` holds `dev:1`.\n- **Log lines per request:** calling the D8 test directly prints three `level=error msg=api_key_lookup_failed error=\"SqlightError(GenericError, \\\"no such table: api_keys\\\", -1)\"` lines, one per key-bearing request (today, DELETE, keys), each with `status=500 tenant=-`.\n- **Token material:** no `level=` line contains either minted plaintext or `gt_` (0 of 9 lines under correct code, 0 of 7 under the mutant).\n\n## Hygiene\n- **FDs**, 3 module runs in one VM: 25→79→133→187, the same +54 per run as e364cc6.\n- **Flake check:** the D8 test passed 24/24.\n- **`defer` teardown:** a passing, failing or panicking body deletes the temp dir, re-raises the original error, and costs +7 fds. When the body stops its own meta owner, the cleanup's stop panics with Noproc and replaces the body's result, the same as r1. No current test does this.\n- Nit N2 (provisioning_timeout leaves 1 dir behind) is unchanged.\n\n## Build and suite\n- Fresh `gleam build --warnings-as-errors`: exit 0, 0 warnings.\n- `gleam format --check src test`: exit 0.\n- Full `gleam test` (optional; I ran it, hub-supervised): `697 passed, no failures`, exit 0, with 3 lookup lines (r1 had 2). A first attempt run from bash hit the 300s timeout and was killed; I discarded it.\n\n## BLOCKING\nNone.\n\n## Nits (do not gate)\n- **N2** (carried): `provisioning_timeout_answers_503_tenant_unavailable_test` leaves its temp dir behind.\n- **N3, N4** (carried; server/src is unchanged): the `KeyRejected` doc misses the retire-purged case, and tenant_db.gleam:113 still cites `meta_db.bump_generation`.\n\n## SCOPE (does not gate)\n- **S1** (carried): `list_keys` answers 500 with no error line when meta.db faults.\n\n## Cleanup\nScratch /tmp/1bk1-edge-A-r2 is deleted. I also removed two empty nix-shell/nix-develop temp pairs left by dead pids during my run window. I believe they were mine but did not confirm it. /tmp is back to 2210 entries, the worktree is clean at 2e5b025, and the worktree list is unchanged. The verdict and Coverage lines were sent to Round1bk1ArchitectC. Model: anthropic/claude-opus-5-5.\n\nCoverage: 19 families, 32 probes, 0 skipped — local://oracle-edge-A-r2.md\nVERDICT: APPROVE"
}
