{
  "verdict": "VERDICT: REJECT",
  "coverage": "Coverage: 14 families, 92 probes, 2 skipped — local://oracle-edge-B.md",
  "matrix": "local://oracle-edge-B.md",
  "scope": [
    {
      "id": "S1",
      "text": "`tenant_registry.evict` (tenant_registry.gleam:486) has 0 production callers after this slice (base: account.gleam:64). Its remaining callers are test_services.gleam:159 and tenant_registry_test ×10. It need not be public production API, because `delete_tenant` owns the only production path. The file is outside this slice."
    },
    {
      "id": "S2",
      "text": "`meta_db.bump_generation` (:1087) and `meta_db.purge_tenant` (:1363) have 0 src callers outside meta_db (base: 1 each, in account.gleam). They need not be exported, because `retire_tenant` does both steps."
    },
    {
      "id": "S3",
      "text": "web's private `KeyAuth` type (web.gleam:722-739) and the `api_key_auth` translation are now a one-to-one mirror of `meta_db.KeyAuth`, with one consumer (`resolve_context`). It need not exist, because `resolve_context` can match `meta_db.KeyAuth` directly."
    }
  ],
  "reply": "Seat B review of slice/1bk1-edge @ a29e1a0 (base 9908cfa). Every bead replay tells base from rev, and every D1-D5 mutant leg reproduces. One blocker: this slice removes the only test that a lapsed account keeps its export.\n\nWhat passed (full rows in the matrix):\n- **Builds and the full suite.** Cold `gleam build --warnings-as-errors` is green on both rev and base. One CI-equivalent `gleam test` on rev: `694 passed, no failures` (base 689, plus 4, plus 1, plus 1, minus 1). `gleam format --check` is clean on the 16 changed files.\n- **PER-6 replays.**\n  - (A) tenants path is a file: base 401, rev 500 with one `tenant_provision_failed` line. Healthz goes degraded after 3 failures on both builds.\n  - (B) broken migration: 401 → 500.\n  - (C) provisioning timeout: 401 → 503 `tenant_unavailable`.\n  - (D) JWT auth mode, a hop the verifier only traced: 401 → 500.\n  - (E) API-key path: 401 → 500.\n- **EDGE-5 replays.** Scenario A: base POST /api/flags 201 and GET /api/account 200 `billing:null` with everything true. Rev: 403 `session_writes_disabled`, 500, GET /api/today 200, export 200, one `entitlements_unavailable` line per request. Scenario B: same result. An unfaulted control matches on both builds.\n- **EDGE-2 race, 200 iterations.** Base: the owner kept the old file in 62/200 and served the deleted marker in 55/200. Rev: 0/200 and 0/200, DELETE 204 ×200, the concurrent GETs 503.\n- **EDGE-1 and EDGE-13.** On base the partial fixture reproduces the verifier's split (account shows billing, checkout and webhook 409). On rev a partial `Billing`, a half `Auth` and `meta_db: None` all fail to compile.\n- **EDGE-10.** Changing the mint prefix to `gk_`: base api_key_policy Failed 7 / Passed 2 (the verifier's numbers), rev 9/9.\n- **EDGE-12.** The grep finds only `config.BillingDisabled`.\n- **D1-D5 legs**, each run under the mutant without the test / with the test / reverted:\n  - D1: 14 green / only `provision_fault_answers_500_not_401_test` red / 18 green.\n  - D2: 4 green (grep = 2) / only `failed_retire_keeps_file_refuses_resolves_and_still_serves_test` red, message \"expected Error(Deleting)… got Ok(Owner…)\" / 5 green. Removing that test's in-window resolve leaves it green under the mutant, so the window assertion is what catches it, as briefed.\n  - D4: 30 / 1 red / 31.\n  - D5: 16 / exactly the 2 dev-mode 401 tests red / 18.\n- **CI invocation.** D1, D2, D4 and D5 mutants applied together under `gleam test` (gleeunit): `58 passed, 5 failures`, exactly the 5 predicted tests.\n- **The new D2 test under load.** 96/96 green across 32 parallel VMs.\n- **Arbiter conditions.** `authenticate_api_key` is called at web.gleam:698 and :802, `retire_tenant` at api/account.gleam:76, `delete_tenant` at api/account.gleam:72; base had 0 of each. web.gleam has 0 hits for `\"gt_\"`, `sha256_hex` and `lookup_api_key_by_hash`, and both functions are private in meta_db (:818, :1474).\n- **Production binary** (`gleam run`). With all four billing variables: boot logs `billing_active`, /api/account returns 200 with billing. With Firebase set and no billing: 401s, webhook 409. With a partial billing environment: boot panics.\n- **Claims measured.** 16 files, +856/−665, one commit by `Dustin <dustin@local>`, worktree clean, md5 of every changed file matches. Every file is inside the ownership list.\n\nBLOCKING\n\nB1. server/test/woodshed_server/export_test.gleam:100 (`billing: None`) and :243-276 (`lapsed_tenant_keeps_export_but_loses_session_writes_test`, still described as a lapse test at :243-248 and in the module doc at :7).\n- FINDING: I added the mutant `use <- bool.guard(!ctx.entitlements.session_writes, wisp.response(402))` at the top of `api/export.get_export`, so a lapsed account loses export. Running eunit `woodshed_server@export_test`: base 9908cfa gives `Failed: 1. Passed: 3.` (the lapsed test); a29e1a0 gives `All 4 tests passed.` No other test requests /api/account/export (grep finds only export_test.gleam:137 and :281). After this slice, nothing in the suite checks that a lapsed account keeps export. docs/10 §2 requires that, and it is why router.gleam:66-69 routes export above `dispatch_api`.\n- EXPLANATION (inferred from the code): with `billing: None`, `web.resolve_entitlements` takes the constant-full `entitlements.resolve` arm, so the `MarkLapsed` row the test writes is never read. The brief's item 4 prescribed `billing: None` and asked the reply to say why each assertion still holds. The reply's answer covers the key_scope refusal and \"export is a GET\"; it does not say the lapse no longer applies.\n- REQUIRED FIX: restore a lapsed-export check that fails under the export-gate mutant. Either option works:\n  - (a) Give export_test's fixture a full `web.Billing` bundle (all four fields, which config accepts), so the lapse is read from meta.db. Probed: 4/4 green, and with the mutant exactly the lapsed test goes red. This needs the architect to revise item 4.\n  - (b) Keep `billing: None` and add a test on `test_services.with_lapsed` that does GET /api/account/export and expects 200; also change the old test's doc so it stops claiming a lapse. Probed: 200, and with the mutant that test goes red.\n\nNITS (do not gate)\n- N1. D4 leg (b): under `Error(_) -> Ok(full)` the test fails because POST /api/flags answers 415 (the test sends no JSON body). The reply and the 1bk.6.2 bead comment say it answered 201. The leg still catches the mutant. Fix: correct the record, or give the POST a `{\"drill_id\":…}` body so the mutant reproduces the incident's 201 (base replay: 201 {\"id\":1}).\n- N2. D3 base hit count: the reply says 21; I measured 22. Its own list names 22 D3 sites plus test_services.gleam:47, which belongs to the 2_592_000 grep.\n- N3. The reply leaves out evidence-contract item 4 (the blast-radius statement) and the final full-suite summary line. I measured the suite myself (694 passed). Deleted lines outside the stated radius: web.gleam:7-12 (module doc), :35-41 (the `api_key_prefix` const, D5's target), :663-678 (KeyAuth docs naming that const), and entitlements.gleam:5-14 (doc of the deleted Bool). The criteria need every one of these.\n- N4. Some arms have no test, though HTTP probes show they behave correctly (Deleting 503, Evicted 503, UnlinkFailed 500 with its line):\n  - resolve_owner: mutants mapping StartFailed, Evicted or Deleting to 401 survive router_auth, tenant_generation and tenant_capacity.\n  - account.gleam: an UnlinkFailed→204 mutant survives.\n  - authenticate_jwt_only: letting KeyRejected fall through survives every suite that owns the path.\n- N5. The `tenant_provision_failed` line (web.gleam:753-758 drops the TenantOpenError) and the `tenant_retire_failed` line (account.gleam:78 `replace_error(Nil)`) carry no cause, and the registry logs nothing. The operator sees the tenant but not the fault. `entitlements_unavailable` and `account_view_failed` do include `string.inspect(e)`. The format matches the brief.\n- N6. The `Services.data_dir` doc (web.gleam:49-51) says it is \"kept for the registry's provisioning config\". The registry takes data_dir from its own Config in main; the only reader of `Services.data_dir` is handlers.gleam:183.\n- N7. The 1bk.7.1 record's reason for keeping `stripe_provider` on Services (\"a dependency seam like clock\") is partly wrong. clock has readers outside billing (web.gleam:447-448, account.gleam:76, :387). `stripe_provider` is read only inside `Some(billing)` (account.gleam:167, :209), and 8 billing-off fixtures build a failure_provider that no path reads. Folding it into `Billing` would make that state unrepresentable. The brief delegated this call, so it does not gate.\n- N8. `web.Billing` repeats `config.BillingActive`'s four fields, and main copies them one by one (woodshed_server.gleam:115-124). S4 prescribed this shape.\n- N9. Docs outside this slice's ownership that this slice made stale: tenant_db.gleam:116 and :150-151, meta_db.gleam:1083-1086, :1199 and :1219 still describe the handler-inline protocol.\n- N10. `fail_closed` (entitlements.gleam:73) has one caller (web.gleam:871) and a one-line body under a four-line doc. The brief offered this option, and it does keep `read_only` private.\n- N11. router_auth_test.gleam:212 now opens meta.db in all 18 tests and never stops the owner. handlers_test and hardening_test stop theirs.\n- N12. web.gleam:564 adds an 11th copy of the `{\"error\":\"internal error\"}` body. It is forced because handlers imports web.\n\nSCOPE (does not gate)\n- S1. `tenant_registry.evict` (tenant_registry.gleam:486) has 0 production callers after this slice (base: account.gleam:64). Its remaining callers are test_services.gleam:159 and tenant_registry_test ×10. It need not be public production API, because `delete_tenant` owns the only production path. The file is outside this slice.\n- S2. `meta_db.bump_generation` (:1087) and `meta_db.purge_tenant` (:1363) have 0 src callers outside meta_db (base: 1 each, in account.gleam). They need not be exported, because `retire_tenant` does both steps.\n- S3. web's private `KeyAuth` type (web.gleam:722-739) and the `api_key_auth` translation are now a one-to-one mirror of `meta_db.KeyAuth`, with one consumer (`resolve_context`). It need not exist, because `resolve_context` can match `meta_db.KeyAuth` directly.\n\nFOLLOW-UPS (outside the verdict)\n- F1. In the rev race replay, 8 of 800 concurrent GETs answered 500 `unhandled_error` (`callee exited … Noproc`): each request held a Ready owner that `delete_tenant` stopped mid-request (base: 1 of 800). This comes from the registry's stop-before-worker behavior (W0-A).\n- F2. `meta_db.authenticate_api_key` turns a lookup DB fault into KeyRejected, so API-key callers get 401 on a server fault (meta_db.gleam:874-877). That is the PER-6 bug class, in W0-B's code.\n\nDesign records: the 1bk.6.2 claims and 1bk.7.4 part 3 match the code. 1bk.7.1 matches except its stripe_provider reason (N7). No `Option` field on Services is decided by another field, and every billing site matches `services.billing` once (web.gleam:859; account.gleam:107, :156, :201, :256).\n\nScratch at /tmp/1bk1-edge-B is deleted, the worktree list is unchanged, 1bk1-edge is clean at a29e1a0, and my smoke servers have exited.\n\nCoverage: 14 families, 92 probes, 2 skipped — local://oracle-edge-B.md\nModel: anthropic/claude-opus-5-5 (harness role @ORACLE)\nVERDICT: REJECT"
}
