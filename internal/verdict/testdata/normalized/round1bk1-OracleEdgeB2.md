{
  "verdict": "VERDICT: REJECT",
  "coverage": "Coverage: 16 families, 74 probes (8 carried), 1 skipped — local://oracle-edge-B-r1.md",
  "matrix": "local://oracle-edge-B-r1.md",
  "scope": [
    {
      "id": "S1",
      "text": "none new; round 0 S1 (tenant_registry.evict) carried"
    }
  ],
  "reply": "Seat B delta re-review of slice/1bk1-edge @ e364cc6 (base a29e1a0). The verdict binds e364cc6. Last round's B1 is closed, and D6 and D7 pass all three legs. D8's code is correct on the built binary, but its test has one gap: a fall-through on the JWT-only path gets past it.\n\n**What held** (full rows in the matrix):\n- **Build and full suite.** A cold `gleam build --warnings-as-errors` is green on both e364cc6 and a29e1a0, and `gleam format --check` is clean on the 6 changed files. One CI-equivalent `gleam test` at e364cc6 printed `697 passed, no failures`, EXIT=0, with 0 warnings. That is 694 + 4 new tests − 1 subsumed = 697.\n- **Round-0 B1 (D6).** I ran both export-gate mutants. Without the new test: 39 of 39 green. With the test: only `lapsed_tenant_keeps_export_test` fails (403 and 402 respectively, instead of 200). Reverted: 40 of 40. The fixture reads the lapse from meta.db: `with_lapsed` has billing active and the clock past the trial end, and the same test gets POST /api/flags 403 `session_writes_disabled` first.\n- **D7.** `printf '%s' 'gt_stored-hash-format-pin' | sha256sum` gives the literal in the test. Dropping the lowercase step: 55 green without the test, only that test red with it, 56 green reverted.\n- **D8 legs.** Mapping the fault back to `KeyRejected`: 54 green without the two new tests, exactly those two red with them, 56 green reverted. The log line on stdout is `level=error msg=api_key_lookup_failed error=\"SqlightError(GenericError, \\\"no such table: api_keys\\\", -1)\"`, once per request, with `tenant=-`.\n- **D8 on the binary (`gleam run`), after renaming `api_keys`.**\n  - Dev mode: six key-bearing requests (today, keys, export, vault POST, DELETE account, garbage `gt_`) all answer 500 with exactly 6 error lines. a29e1a0 answers 401 to all six with no line. Requests with no bearer or a non-key bearer still get the dev tenant (200).\n  - JWT mode: a key on today and on /api/account/keys gets 500/500 with 2 lines. a29e1a0 gives 401/401.\n- **F4.** The grep finds 0 hits. Four mutants of `bump_generation_on`/`purge_tenant_on` fail on e364cc6 as they did on a29e1a0: latest-wins 1→1, null-erases 2→2, keep-keys 3→2, purge-generation 2→5. The lines the reply cites at :580, :587 and :589 are there.\n- **F5 and D5.** Both greps find 0 hits. D5 legs: 54 green / exactly the 2 dev-mode 401 tests red / green reverted.\n- **D1-D5 leg (c).** 124 of 124 tests pass across the 10 owning modules (19, 2, 5, 4, 31, 5, 9, 23, 23, 3).\n- **Nits the fix round addressed.**\n  - D4: the exact recorded mutant now fails with \"201 should equal 403\".\n  - The provision cause is in the binary log: `tenant_provision_failed tenant=dev error=ProvisionFailed(DataDirUnavailable(Eexist))`.\n- **Radius.** Every deleted line is in a named site. The out-of-site lines are all accepted, each for a necessary reason:\n  - meta_db -25/-311/-443/-1041/-1226 name the deleted public functions.\n  - web -385/-391 are the doc wording and the F3 sentence.\n  - web -445,2 changed because `KeyIdentity` is now built in `resolve_context`.\n  - web -526,3/-548/-561/-563 are the variant field for the cause and the new arm.\n  - The `account.gleam:78` cause is not logged. That was optional, and the retire seam `fn(String) -> Result(Retired, Nil)` drops the error before account sees it.\n- **Records.**\n  - The S3 revision on 1bk.7.6 matches the code: the four variants, the \"iff\" (key_hash is unique, so the >1-row arm cannot happen), 500 plus one line, no tenant field, and no web mirror.\n  - The new stripe_provider reason in 1bk.7.1 matches: the provider is read only at account.gleam:167 and :209, while clock has readers outside billing.\n  - The 1bk.6.2 correction matches.\n- **Design of the new `AuthError` arm and `internal_error()`.**\n  - `KeyLookupFailed` and `ApiKeyLookupFailed` each have two construction sites and one consumer.\n  - `internal_error()` has two callers and holds the one 500 body in web.\n  - Copies of `\"internal error\"` in src stay at 11: the new arm adds none.\n  - The web module still hides one thing (how credentials and auth failures map to a status and log line), now with one more arm.\n\n**BLOCKING**\n\nB1. server/test/woodshed_server/router_auth_test.gleam:295-333 (`dev_mode_key_lookup_fault_is_500_not_401_test`: the /api/account/keys leg at :326-332 and the doc at :300-301). This is the only test that guards web.gleam:720, the fault arm in `authenticate_jwt_only`.\n- **FINDING:**\n  - I mutated web.gleam:720 to `meta_db.KeyLookupFailed(_) -> resolve_jwt(req, services)`, so `authenticate_jwt_only` falls through on a lookup fault. Running eunit router_auth, meta_db, api_key_policy, export and router_billing gives `All 87 tests passed.` A bash-only re-run on a fresh clone gives the same result.\n  - The only router-level fault injection in server/test is router_auth_test.gleam:315, so no other test can see this mutant.\n  - The same mutant in `resolve_context` (web.gleam:782) does turn the test red (\"200 should equal 500\").\n  - On the mutant binary in JWT mode, with the fault, key-bearing GET /api/account/keys, DELETE /api/account and POST /api/account/billing/checkout answer 401/401/401 with no `api_key_lookup_failed` line. That is F3's defect on the JWT-only routes, and the suite stays green.\n  - D8 names this property for `authenticate_jwt_only` (\"NEVER falls through to JWT or the dev tenant\"). The test doc (\"never a fall-through to the dev tenant (which would answer 200)\") and the reply (\"A dev-tenant fall-through would answer 200\") both say it is guarded. That holds for /api/today, not for /api/account/keys.\n- **EXPLANATION (probed):** the renamed table also breaks the key-list query. In dev mode the fall-through reaches `api/account.list_keys`, whose `Error(_)` arm (account.gleam:346-349) answers the same 500 `{\"error\":\"internal error\"}` and logs nothing. On the dev binary under the fault, a non-key bearer on /api/account/keys shows `status=500 tenant=dev` with no error line. So the keys leg sees the same response under the correct code and under the fall-through.\n- **REQUIRED FIX:**\n  - In the D8 router test, add one key-bearing request on a JWT-only route whose dev-tenant handler does not read `api_keys`, and assert 500 plus the body. For example POST /api/account/billing/checkout. I probed this: router_auth is 19 of 19 green, and under the fall-through mutant that test fails with \"409 should equal 500\".\n  - Correct the doc at :300-301 so it says what each leg checks.\n  - Leg (b) is the web.gleam:720 fall-through mutant.\n\n**NITS** (do not gate)\n- N1. tenant_db.gleam:113 now names `meta_db.bump_generation`, which F4 removed; the fix list leaves it to the polish pass. meta_db.gleam:1123's error text also still says \"bump_generation\".\n- N2. The src tree still holds 11 copies of `{\"error\":\"internal error\"}`, the same count as before; carried from round 0 N12. handlers imports web, so web cannot use `handlers.internal_error`.\n- N3. The lookup-fault decision is written twice (web.gleam:720 and :782), which is the shape S3 and F5 prescribed. B1 shows the cost: the :720 copy was the one no test covered. An `api_key_auth` that returned `Result(Option(ApiKeyRecord), AuthError)` would hold the rejection and fault decisions in one place. This notes the cost only; no change asked.\n- N4. Carried from round 0 N4 (these arms are unchanged and I did not re-probe them): mutants mapping StartFailed, Evicted or Deleting to 401, and UnlinkFailed to 204, still pass the suite.\n\n**SCOPE** (does not gate)\n- None new. Round 0's S1 (`tenant_registry.evict` has no production callers) is carried; that file is untouched.\n\n**FOLLOW-UP** (outside the verdict)\n- `api/account.list_keys` answers a store fault with 500 and no log line (account.gleam:346-349). This is pre-existing; a29e1a0 behaves the same.\n\n**Isolation**\n- The Python eval kernel I used to drive mutant clones and HTTP probes was shared with OracleErrorsB2; I noticed at 03:15.\n- Every kernel-driven result printed paths and test names that exist only in these edge trees. I re-ran the blocker mutant and the exact 1bk.6.2 mutant from bash with absolute paths. ErrorsB2 confirmed none of its cells touched my paths.\n- `nix develop` on the scratch path flakes copied those trees into the nix store; garbage collection can remove them.\n\n**Cleanup**\n- /tmp/1bk1-edge-B-r1 is deleted and all six smoke servers have exited.\n- The worktree list has the same entries. track/1bk moved from 04736b4 to 1e00de0; I did not do that.\n- 1bk1-edge is clean at e364cc6.\n\nCoverage: 16 families, 74 probes (8 carried), 1 skipped — local://oracle-edge-B-r1.md\nModel: anthropic/claude-opus-5-5 (harness role @ORACLE)\nVERDICT: REJECT"
}
