{
  "verdict": "VERDICT: REJECT",
  "coverage": "13 families, 42 probes, 0 skipped — local://oracle-registry-B.md",
  "matrix": "local://oracle-registry-B.md",
  "scope": [
    {
      "id": "S1",
      "text": "`Retired.generation` has zero readers. The registry binds it as `_generation` in `start_delete_worker`. It is seam-pinned, so whether it should exist is the architect's ruling."
    }
  ],
  "reply": "Seat B (acceptance-replay, then design-review) on slice/1bk1-registry @ 706e33f, base 929d84e. Every probe ran in scratch copies under /tmp/1bk1-registry-B, each built from `git archive` of its commit, with TMPDIR set per run under the scratch dir. The scratch dir is deleted, no worktrees or branches were created, and nothing was written to bd.\n\n## BLOCKING\n\n**B1: Seam S1 semantics (1) and A2 are violated on the Provisioning path. delete_tenant hands out, and leaks, a live owner on the generation it unlinks. This is the EDGE-2 incident through the new API.**\n- Where: tenant_registry.gleam:1040-1053 (the `handle_delete` Provisioning arm parks in `state.parked_deletes` only), :842-856 (the `[], Ok(owner)` arm), and :1192-1222 (`continue_after_settle`).\n- Finding (probe P1, scratch module): one resolve was parked on a gated Provisioning attempt. Then `delete_tenant(reg, \"dp\", bumping_retire)` was called, a second resolve was issued after it, and the gate was released. Observed:\n  - `resolve1 (parked before delete): Ok(owner alive=True main=…/tenants/dp.db)`\n  - `resolve2 (issued after delete_tenant): Ok(owner alive=True main=…/tenants/dp.db)`\n  - `delete_tenant: Ok(Ok(Nil))`\n  - `opened owner alive after delete returned: True`\n  - `owner main file on disk: Ok(False)`\n  - `write through handed-out owner after delete: Ok(Nil)`\n- Replay: 200 iterations, tenant not cached, 2 resolves, then the delete, then 2 resolves. A hit is a live owner on a non-current generation after the delete returns. Head: `200/200` (run twice). The base handler protocol (evict→bump→unlink) scored `0/200` on the same shape. On the verifier's Ready-path shape the slice does fix the incident: base `28/200`, head `0/200`.\n- Mechanism (probed): in mutant mechB the Provisioning arm also parks an evictor in the attempt's `pending_evicts`. Both resolves then get `Error(Evicted)` and the owner is dead. So the cause is that a parked delete does not mark the in-flight attempt the way evict does.\n- Explanation (inference from reading the code): the `[], Ok(owner)` arm replies `Ok(owner)` to every waiter and installs Ready. `continue_after_settle` then overwrites that Ready with DeletingTenant without calling `connection_owner.stop`. The owner leaves the dict, so the idle sweep can never reap it.\n- Required fix: a delete that arrives on a Provisioning tenant must settle that attempt the way evict does. Waiters, including resolves that arrive while the delete is parked, get `Error(Evicted)` or `Error(Deleting)`, and the just-opened owner is stopped before the worker starts. Add a test on this path with a gated open that fails under the current code.\n\n**B2: A late ProvisionDone(Ok owner) that lands on a DeletingTenant entry is dropped without closing the owner. This is a connection and process leak on the old generation.**\n- Where: tenant_registry.gleam:771 `Ok(DeletingTenant(..)) -> actor.continue(state)`. The comment at :767-770 says \"a delete worker never produces one\", but ProvisionDone comes from provisioning workers.\n- Finding (probe P2): provision_timeout_ms=100 with a gated open gave `Error(ProvisioningTimedOut)`. A delete_tenant followed, with retire blocked on a message, and then the open was released. Observed:\n  - `late owner stopped within 2s while Deleting: False`\n  - `delete_tenant: Ok(Ok(Nil))`\n  - `late owner stopped within 2s after delete returned: False`\n  - `late owner main file: …/tenants/sl.db`\n- Base analog: `late_provisioning_completion_after_timeout_is_dropped_and_owner_shut_down_test` (the owner is closed when the dict is empty) is green.\n- Mechanism (probed): in mutant mechA that arm calls `close_leaked_owner(result)`, and the owner is then stopped (`True`/`True`).\n- Required fix: close the leaked owner in that arm, as the Error(Nil) and Ready arms already do, and correct the comment.\n\n## Criteria legs (all reproduced independently; a leg-a run renames the tests out, a leg-b run keeps them)\n- **A0:**\n  - `grep -c '\"/tmp/woodshed-'` gives 0 at head and 8 at base.\n  - Two head copies with a shared TMPDIR passed 3 of 3 rounds: 6/6 `All 29 tests passed.`\n  - The same setup on base failed 2 of 3 rounds (`Failed: 1 Passed: 24`).\n- **A1:**\n  - Criterion mutant: leg b turns exactly health_resets… and open_faults_split… red (`Failed: 2 Passed: 27`); leg a gives `All 27`.\n  - Extra mutant (InvalidTenantId counted as a failure): same two tests red.\n- **A2:** leg b turns only resolve_during_delete_window… red (`Failed: 1 Passed: 28`); leg a gives `All 28`. The test only covers the Ready path, which is why B1 passes it.\n- **A3:** leg b turns only delete_tenant_failure_ordering… red (`Failed: 1 Passed: 28`); leg a gives `All 28`.\n- **A4:** leg b turns only remove_files_fails_when_the_main_file_survives red (`Failed: 1 Passed: 14`); leg a gives `All 13`.\n- **Preserve clauses:** green on base and head in the module runs.\n- **Full suite:** CI-style `gleam test` at head gives exit=0, `676 passed, no failures`, which matches the reply.\n- **Other reply claims checked:** the diff stat of 102 deletions matches; all 4 files are inside the ownership list; grep finds no remaining `InvalidTenant(` consumers; the four changed files pass `gleam format --check`.\n\n## Nits\n- **N1:** `start_delete_worker` maps `RemoveInvalidTenantId` to `Error(RetireFailed)` (`Error(_) -> Error(RetireFailed)`) after retire has already succeeded. P3: `delete_tenant(\"../x\")` gives `Error(RetireFailed)` while the generation moved 0→1, which contradicts the DeleteError doc (\"generation untouched\"). Through HTTP this arm is unreachable, because authenticate resolves the tenant first (web.gleam:689, 759, 780).\n- **N2:** there is no deadline on a wedged retire (P4). The resolves were refused `Deleting` 10/10 and the refusals continued across 8 sweeps. evict gave no reply within 500 ms and delete_tenant (call_forever) gave none within 2 s. Health stayed Healthy and other tenants were unaffected. S1 does not require a deadline.\n- **N3:** when several deletes park on one Provisioning attempt, the retire that runs is the last arrival's (list prepend at :1043-1046, head taken at :1207). P11 printed `retire that ran: \"second\"`, but the doc says the first request wins.\n- **N4:** part 3/4 of the bead design record says main wires `retire` and counts woodshed_server.gleam in the rewrite cost. The plan (premortem R9) has api/account build the thunk and leaves main unchanged.\n- **N5:** comment damage at tenant_registry_test.gleam in `late_provision_done_after_retry…`: the line now reads \"// down explicitly)\".\n- **N6:** `remove_files_fails_when_the_main_file_survives_test` leaves a 0o500 directory under TMPDIR when it fails, because the permissions are restored only after the assertion.\n\n## SCOPE\n- **S1:** `Retired.generation` has zero readers. The registry binds it as `_generation` in `start_delete_worker`. It is seam-pinned, so whether it should exist is the architect's ruling.\n\n## Design-review\n- Depth is good: one function and two small types hide about 230 lines of lifecycle.\n- The rewrite probe finds no leak outside the module.\n- delete_tenant, Retired and DeleteError have 0 callers in the tree; the call site is owned by sibling slice W1-D (round-1.md §2).\n- The `-wal`/`-shm` suffixes live only in tenant_db; the copies in account.gleam are removed by W1-D's D2.\n- Keeping parked deletes in a separate `state.parked_deletes` dict, so the in-flight attempt cannot know it is doomed, is the structural root of B1 (inference, supported by the mechB probe).\n\nCoverage: 13 families, 42 probes, 0 skipped — local://oracle-registry-B.md\n\nVERDICT: REJECT"
}
