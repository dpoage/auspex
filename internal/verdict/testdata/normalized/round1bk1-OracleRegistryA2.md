{
  "verdict": "VERDICT: APPROVE",
  "coverage": "Coverage: 15 families, 66 probes, 0 skipped — local://oracle-registry-A-r1.md",
  "matrix": "local://oracle-registry-A-r1.md",
  "scope": [
    {
      "id": "S1",
      "text": "**Delete arriving before a dead owner's Down (P9, carried).** The registry crashes at `handle_delete:1062` (706e33f `:1037`). Evict at base has the same crash."
    },
    {
      "id": "S2",
      "text": "**Dead owner inside a stray ProvisionDone (N12).** I injected a ProvisionDone carrying a dead owner:\n  - On a313609 it now crashes the registry when it lands on a Deleting slot, at `handle_provision_done:970`.\n  - 706e33f already crashed the same way on an empty slot (`:945`).\n  - This is the same crash as P9, at one more place. I only reached it by injecting the message, because the late owner is linked to a provisioner that exits normally."
    },
    {
      "id": "S3",
      "text": "**A deleted generation's file comes back (N4z).** A provisioner that read generation 0, then timed out, opens after `delete_tenant` returned `Ok`. This recreates an empty `t.db` (0 rows; the late owner is closed; `tenant_count` stays 1; the next resolve uses generation 1). 706e33f behaves identically. I did not run it on base because base has no `delete_tenant`."
    }
  ],
  "reply": "# Seat A (bug-hunt), delta re-review: slice/1bk1-registry @ a313609\n\nModel: anthropic/claude-opus-5-5 (oracle seat A, bug-hunt).\n\nEverything ran in /tmp/1bk1-registry-A-r1/, which I have deleted. It held a `git archive` of a313609 and one of 706e33f, each with the worktree's `build/packages` copied in and the first-party caches purged. `TMPDIR` pointed under the scratch dir for every `nix develop` run, and each run got its own erl VM. Dev shell: gleam 1.18.1, erlang 28. I took the probe source from local://oracle-registry-A-probes.gleam plus my earlier p3–p11b probes, changed them to the one-field `Retired`, and added a new n-series. Every run on 706e33f below uses the same probe module, so it shows the probe tells the two commits apart.\n\n## BLOCKING\nNone.\n\n## The two blockers, re-run\n- **F1 (P1).** On a313609: resolve A (sent before delete_tenant) and resolve B (sent after) both return `Error(Evicted)`. Resolve C, sent during retire, returns `Error(Deleting)`. The delete returns `Ok(Nil)`, generation 0 is unlinked, and the next resolve opens `t.g1.db` with 0 rows. On 706e33f, A and B both got `Ok(Owner <0.95.0>)`, that owner was still alive after the delete returned, and it read 2 rows from the unlinked file.\n  - N10 reads the opened owner's pid inside `retire`. On a313609 the owner is already dead when the delete worker starts (`owner alive at retire start: Ok(False)`); on 706e33f it was `True`.\n  - The 200-trial uncached replay has 0/200 hits on a313609 (Deleting 156, Ok 544, stale 0) and 23/200 on 706e33f. The cached replay is 0/200 on both.\n- **F2 (P2).** The late owner is gone 500ms after it lands on the Deleting slot and still gone after the delete returns; on 706e33f it was alive both times and still read 1 deleted row. The empty-slot control closes the owner on both commits.\n  - N4 is the same race reached through a timeout: a delete parks, the attempt times out, and the stuck open then lands on the Deleting slot. The owner is closed on a313609 and alive on 706e33f.\n\n## Criterion legs\nEach leg is a targeted eunit run of `tenant_registry_test` (31 tests) or `tenant_db_test`. Leg a = mutant with the target test renamed away, leg b = mutant with the test, leg c = unmutated a313609.\n\n| Criterion / mutant | leg a | leg b | leg c |\n|---|---|---|---|\n| A2', CAS mutant (Provisioning delete replies `Ok(Nil)` at once) | All 30 passed | Failed 1: `delete_during_in_flight_provisioning_hands_out_no_owner_test` | All 31 passed |\n| A2', 706e33f shape: `[] -> False` in `unwanted` | All 30 passed | Failed 1: same test | All 31 passed |\n| A2', 706e33f's whole source with `Retired` narrowed | All 29 passed | Failed 2: the A2' test and `late_provision_done_landing_on_a_delete_stops_its_owner_test` | All 31 passed |\n| A5, `close_leaked_owner` removed from the DeletingTenant arm | All 30 passed | Failed 1: the A5 test | All 31 passed |\n\nI re-ran the earlier legs because they sit on files this diff touches:\n- A1, A3, A4 and X behave exactly as last round.\n- A2's leg a is now red instead of green. The new A2' test also catches that mutant, so coverage went up.\n- Tests the brief asked to stay green all pass: `evict_during_in_flight_provisioning_closes_connection_without_leaking_test`, both two-evictor tests, `health_resets_on_a_successful_provisioning_test`, the A3 test, both A4 tests, and the other delete tests.\n- One full `gleam test` run: `678 passed, no failures`, exit 0.\n- The cold build with `--warnings-as-errors` had 0 warnings.\n\n## F3, parked-delete order, id validation, test cleanup\n- **F3.** `Retired(previous_generation: Int)` has exactly one field. `grep -c previous_generation` gives 3 in the registry source and 5 in its test. No two-field constructor is left anywhere in server/.\n- **Parked-delete order.** Three deletes parked on one open, with distinct `retire` thunks (first succeeds, second and third fail). On a313609 only `[\"FIRST(ok)\"]` ran, all three callers got `Ok(Nil)`, and the generation is 1. On 706e33f only `[\"THIRD(fail)\"]` ran and everyone got RetireFailed. The code now matches the doc.\n- **Id validation.** I sent 15 ids through both `resolve` and `delete_tenant`.\n  - The 11 invalid ids (`../x`, empty, `a.b`, `t\\n`, `é`, `a b`, `/abs`, a NUL byte, leading or trailing space, 129 chars): `resolve` returns InvalidTenantId, `delete_tenant` returns RetireFailed, and `retire` never runs.\n  - The 4 valid ids (128 chars, `-`, `_`, `T9-_z`) go through normally on both calls. The two paths agree on every id.\n  - On 706e33f, `retire` ran for all 11 invalid ids.\n- **Test cleanup.** With the A4 mutant making `remove_files_fails_when_the_main_file_survives_test` fail, the leftover directory is 0o700 on a313609 and 0o500 on 706e33f. The damaged comment is fixed.\n\n## Deleted lines vs the fix list's allowed files and areas\nEvery deleted line in `git diff -U0 706e33f..a313609` falls inside what the fix list allowed:\n- **Source:** docs, the `Retired` type (F3), the `delete_tenant` id check, the `:767-771` arm (F2), the settle arm in `handle_provision_done` (F1), the parked-delete prepend, and the `start_delete_worker` destructure and `Error(_)` arm.\n- **Tests:** the permission restore; the comment fix; the four `Retired` call sites; and 14 lines of `with_registry_on`'s Config block, moved unchanged into the new `with_registry_on_hooks` helper that the two new tests use.\n\n## New state transitions in the fix\n- **Open fails with a delete parked (N3):** the waiters get `GenerationUnavailable`, the delete runs and returns `Ok`, and the next resolve is on generation 1.\n- **Timeout with a delete parked (N4):** see F2 above.\n- **Provisioner crash (N5):** the waiters get `ProvisioningCrashed`, the delete returns `Ok`, and health stays Healthy.\n- **Evict and delete both parked (N6):** the waiters get Evicted, the evict replies at settle, and the owner is dead before the delete is released.\n- **Parked delete whose retire fails (N7):** the waiter gets Evicted, the owner is stopped, the delete returns RetireFailed, and the next resolve reopens generation 0 with its 1 row intact. On 706e33f the waiter got an owner that stayed alive.\n- **Late ProvisionDone on other slot states (N8):** landing on another attempt's Provisioning slot or on a Ready slot, the late owner is closed and the current owner is untouched.\n- **Leak stress (N11):** 24 workers × 40 random resolve/delete/evict calls, with a 40ms deadline so timeouts and late ProvisionDones are frequent, run twice. Afterwards no owner is alive except each tenant's Ready owner (0/41 and 0/47 on a313609; 10/44 and 9/42 on 706e33f), and every Ready owner is on its tenant's current generation.\n- **Earlier probes re-run:** P3, P4, P6b, P7, P8, P10, P11 and P11b behave as last round.\n\n## Nits (do not gate)\n1. **A2' waiter wording.** A2' says parked waiters \"get `Error(Evicted)` or `Error(Deleting)`\". That holds only when the open succeeds. When the parked attempt fails, times out or crashes, waiters get its own error instead: `GenerationUnavailable`, `ProvisioningTimedOut` or `ProvisioningCrashed` (N3, N4, N5). No owner is ever handed out, and this is how evict already behaves. The same wording is in the module doc at `tenant_registry.gleam:52` and the State doc at `:338`. The orchestrator should decide which reading A2' intends.\n2. **Three new behaviours have no test.** A test suite that still passes under a mutant means nothing tests that behaviour.\n   - Parked-delete order: the ORD mutant (prepend again) passes all 31.\n   - Id validation: the VAL mutant (check removed) passes all 31.\n   - \"Owner stopped before the delete worker starts\": the STOPASYNC mutant (delete path stops the owner 50ms after settle) passes all 31. The A2' test's doc comment claims it checks this, but it only waits up to 2s for the owner to die. The code does do it (N10).\n   - The NOSTOP mutant (owner never stopped) is caught by the A2' test.\n3. **`ResolveError.Evicted` doc (`:217`)** still names only `evict` as the source, but `delete_tenant` now produces it too.\n4. **Comment on the `Error(Nil)` arm (`:767-772`)** says \"Shouldn't happen … only `handle_provision_done` ever removes\" a Provisioning entry. That arm is hit on every late ProvisionDone after a timeout. The comment was already there at base (929d84e:604).\n5. **Stuck `retire` has no deadline** (P4, carried from last round). The fix list said not to add one.\n\n## SCOPE (do not gate)\n- **Delete arriving before a dead owner's Down (P9, carried).** The registry crashes at `handle_delete:1062` (706e33f `:1037`). Evict at base has the same crash.\n- **Dead owner inside a stray ProvisionDone (N12).** I injected a ProvisionDone carrying a dead owner:\n  - On a313609 it now crashes the registry when it lands on a Deleting slot, at `handle_provision_done:970`.\n  - 706e33f already crashed the same way on an empty slot (`:945`).\n  - This is the same crash as P9, at one more place. I only reached it by injecting the message, because the late owner is linked to a provisioner that exits normally.\n- **A deleted generation's file comes back (N4z).** A provisioner that read generation 0, then timed out, opens after `delete_tenant` returned `Ok`. This recreates an empty `t.db` (0 rows; the late owner is closed; `tenant_count` stays 1; the next resolve uses generation 1). 706e33f behaves identically. I did not run it on base because base has no `delete_tenant`.\n\nCleanup: /tmp/1bk1-registry-A-r1/ is deleted, both supervised processes have exited, the worktree is clean at a313609, and the worktree list is unchanged.\n\nCoverage: 15 families, 66 probes, 0 skipped — local://oracle-registry-A-r1.md\n\nVERDICT: APPROVE (binds a313609)"
}
