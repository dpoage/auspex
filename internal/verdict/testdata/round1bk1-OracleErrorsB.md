{
  "verdict": "VERDICT: REJECT",
  "coverage": "Coverage: 26 families, 65 probes, 2 skipped — local://oracle-errors-B.md",
  "binding_commit": "7037166 (70371669a2980aca92692b5b5f6e7e886fd7e151), base 9908cfa",
  "model": "anthropic/claude-opus-5-5 (harness role @ORACLE)",
  "reply": "Seat B review of slice/1bk1-errors @ 7037166 (base 9908cfa): acceptance replay first, then design review.\n\nIsolation: I exported both trees with `git archive` into /tmp/1bk1-errors-B/. The first-party build dirs were deleted so every build was cold, and only the dep caches were copied from the worktree. TMPDIR pointed inside the scratch dir and I used my own kernel. Every wire probe went through `test_services.request`, which is `router.handle_request`, from a scratch probe module run with `erl -eval`. I captured stdout and counted `msg=invariant_violated` lines.\n\n**What holds (all measured):**\n- **Incident replay.** I ran the EDGE-4 ghost-row repro verbatim: minimum sitting, `ghost-removed-drill` inserted through fixture.owner, then POST /api/sessions/1/drills.\n  - Base: 404 {\"error\":\"not found\"} with no error line.\n  - 7037166: 500 {\"error\":\"internal error\"} with exactly one `level=error msg=invariant_violated` line.\n  - Three other content-drift cases split the same way (base 404, 7037166 500 plus one log line): a composed drill deleted from content.db, a drill's module_id set to a missing module in log_drill, and the same module drift on PUT /api/drills/:id/mastery.\n  - Client-supplied ids still return the named 404: unknown drill route param, unknown session on /drills, unknown session on /complete.\n- **E1 and E2 mutant legs.** I reproduced all three legs of each.\n  - Leg a, full `gleam test` with both mutants and the base test files: 688 passed, no failures. The hole exists at suite level the way CI runs it.\n  - Leg b, targeted modules: exactly log_drill_of_a_drill_removed_from_content_returns_500_test fails for E1 (Failed: 1, Passed: 45). Exactly advance_module_after_mastery_unknown_module_id_is_a_no_op_test fails for E2 (Failed: 1, Passed: 25).\n  - Leg c: 46/46 and 26/26 pass.\n- **Greps and gates.** The E1 and E3 greps give 0 in both the brief's form and the plan's form. The cold `--warnings-as-errors` build is green, `gleam format --check` is clean, and the full `gleam test` gives 690 passed, no failures.\n- **Preserve clause.** The brief's line numbers are from 929d84e and point at other tests at 9908cfa, so I mapped them by name at 929d84e. All 16 tests (the 15 404s plus library vault 500) have byte-identical bodies at base and 7037166 and pass on both. So do the two E3 wire tests.\n- **Seam.** The `store_error_response` and `invariant_error` signatures and bodies match R3 verbatim.\n- **Ownership and excursions.** All 18 touched files are on the ownership list. Each change outside the named line ranges is needed:\n  - import swaps forced by the new arms and `--warnings-as-errors`\n  - the progression body re-indent, which is whitespace-only under `git diff -w`\n  - the library doc merge\n  - the drill_mastery variant, placed inside the type; the brief's range is stale\n  - the two mandated builders in handlers\n  - today.gleam:1558\n  None of them changes a reachable wire status. POST /api/sessions/999999/complete gives 404 \"session not found\" on both builds.\n- **Claims.** The hash, the diffstat (18 files, +240/-145), 690 = 689 + 2 - 1, the before count 126 = 20+20+5+18+18+45, and \"exactly one log line\" all reproduce.\n\n**BLOCKING**\n\nB1. The diff moves a server-side miss on /api/onboarding from 500 to 404, and the new handlers doc claims that cannot happen. Locations: `api/onboarding.gleam:26` (`service.Store(e) -> handlers.store_error_response(e)`) and the contract text at `handlers.gleam:35-41` and `:67-69`.\n- FINDING:\n  - I deleted the tenant's `onboarding` row, which the 0020 migration seeds, through fixture.owner (`delete from onboarding;`).\n  - GET /api/onboarding: base 9908cfa returns 500 {\"error\":\"internal error\"}; 7037166 returns 404 {\"error\":\"not found\"}.\n  - PUT /api/onboarding {completed:true,tier:\"full\"}: base 500, 7037166 404 {\"error\":\"not found\"}.\n  - Zero `level=error` lines on either build.\n  - handlers.gleam:38-41 says a server-side inconsistency \"always answers 500 with an `invariant_violated` error log and never masquerades as a client 404\". handlers.gleam:67-69 says \"A miss the SERVER derived never arrives as a store error at all\".\n  - A second counterexample to :38-41, identical on base: with the `settings` row deleted, GET /api/today returns 404 {\"error\":\"session not found\"} on both builds.\n- EXPLANATION (inferred from source; the chain itself was not probed separately):\n  - service/onboarding.gleam:21-23 `wrap_store` passes a store NotFound through as `Store(NotFound)`. store/onboarding get and set return NotFound only when the server-owned singleton is missing. The new delegation renders that as 404.\n  - curriculum, drill_mastery, library and packs have the same pass-through wrap_store. Their only NotFound-returning store calls are upserts whose RETURNING cannot come back empty, so those paths are unreachable.\n  - No source path deletes the row (grep `delete from onboarding` in src returns nothing). The state comes only from a damaged tenant DB, the same reachability class as from_packs_error, which the brief did require classifying.\n  - This is the harm EDGE-4 names (\"a server-side inconsistency that surfaces as 404\"). E1's property says \"for every /api/* route\", but the brief's list of mint sites left this one out.\n- REQUIRED FIX (the architect picks one):\n  - (a) Classify at the source. service/onboarding maps the seeded-singleton miss to an onboarding `Invariant(detail)`, and api/onboarding sends it to `handlers.invariant_error`. That file is owned by the slice but outside E1's named range, so it needs a range extension.\n  - (b) Rule seeded-singleton misses out of E1's scope and rewrite handlers.gleam:35-41 and :64-69 to state the contract the binary actually keeps. A store NotFound answers 404 whatever its origin, and server-derived misses are classified only at the named mint sites. Record the onboarding 500→404 change on 1bk.6.1.\n\n**Nits (non-gating)**\n\n- N1. The 1bk.6.1 design record and the reply's table say review.gleam:559-562's module_id \"comes from the persisted rewind proposal\". That provenance is wrong.\n  - review.gleam:526 computes `rewind.rewind_proposal(content, snapshot)` on each request.\n  - rewind.gleam:57-60 and :112 take module_id from the same `content.modules`, so the find at review.gleam:560 can never miss.\n  - The class (server-derived) is right; the arm is unreachable.\n- N2. The guitartime-diy record says every caller passes a module id taken from the content object it hands in. That is not literally true for library.gleam (base :383-393).\n  - module_id comes from the first content fetch (find_*_item_with_module), and a second `today_service.curriculum` fetch is what gets passed in.\n  - The two can differ only if the active curriculum changes between them, e.g. a concurrent pack toggle [INFERENCE, not probed]. In that case the rollup is now skipped silently instead of returning 500.\n- N3. The `[] -> Error(SessionNotFound)` arm at today.gleam:1586 cannot run.\n  - The same transaction already ran `session_store.get` at :1391, and wrap_store maps a miss there.\n  - The brief's own rule for unreachable guards, applied to from_packs_error, would make it Invariant.\n  - Harmless either way: the wire result is unchanged.\n- N4. Design, error classification: Invariant is an impossible-state error value that needs 8 arms across 6 api modules. This is the R3-ruled shape, so it is recorded as a note only.\n- N5. The brief's line numbers (the preserve list, drill_mastery 28-34) date from 929d84e. The implementer ran whole modules, so coverage held; I mapped the tests by name.\n\n**SCOPE (non-gating)**\n\n- S1. Six wrapped `Today(Invariant)` arms never run in shipped code: api/assess.gleam:65, api/curriculum.gleam:75 and :79, api/drills.gleam:27, api/library.gleam:35, api/review.gleam:45.\n  - The only reachable producer of today_service.Invariant is log_drill (today.gleam:1157-1175). Its only consumer is api/today (router.gleam:159 → api/today.gleam:575).\n  - The other producers never fire. from_packs_error (:234-237) can't get these errors: packs.gleam builds PackNotFound/PremiumRequired only in set_enabled (:184, :188). review.gleam:560 is unreachable (see N1).\n  - These arms need not exist. Without them those paths fall to `Today(_) -> internal_error()` and still answer 500. The brief mandated them, so this is the orchestrator's ruling.\n\n**Follow-up, outside the verdict (identical on base):** today.gleam:75-83 `wrap_store` rewrites every store NotFound into SessionNotFound, including misses on the seeded singleton rows (settings, rotation, review_state, rewind_state). So GET /api/today answers 404 \"session not found\" when the settings row is missing. The brief kept rewriting wrappers on purpose. This is a candidate bead under R6.\n\nScratch dir /tmp/1bk1-errors-B deleted. I created no worktrees; the /tmp/1bk1-edge/scratch worktree that vanished from the list belongs to another seat. The reviewed worktree is untouched: status clean, HEAD 7037166.\n\nModel: anthropic/claude-opus-5-5 (harness role @ORACLE).\n\nCoverage: 26 families, 65 probes, 2 skipped — local://oracle-errors-B.md\nVERDICT: REJECT"
}