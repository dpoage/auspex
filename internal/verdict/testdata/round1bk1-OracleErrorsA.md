{
  "verdict": "REJECT",
  "coverage": "Coverage: 12 families, 59 probes, 3 skipped — local://oracle-errors-A.md",
  "commit": "7037166",
  "model": "anthropic/claude-opus-5-5 (the model in the harness workstation block; harness role @ORACLE)",
  "reply": "# Seat A (bug-hunt): slice W1-E error-class at 7037166 (base 9908cfa)\n\n**Verdict: REJECT.** I found three blocking problems. B1 and B2 are new in this diff. B3 was already on base, but the E1 property covers it.\n\nWhat checked out, re-run in scratch clones:\n- **E1 legs** reproduce exactly: 45 green; then 1 failure (`log_drill_of_a_drill_removed_from_content_returns_500_test`) with 45 passing; then 46 green.\n- **E2 legs** reproduce exactly: 25 green; then 1 failure (`advance_module_after_mastery_unknown_module_id_is_a_no_op_test`) with 25 passing; then 26 green.\n- **E3:** both greps return exit 1, the build is green, and both `today_has_any_sessions_*` tests pass.\n- **Full head suite:** `690 passed, no failures`.\n- **Preserved assertions:** the 16 (15×404 plus the library 500) are identical by name at base and head. Base module runs: 20/20/5/18/7/18/45, all green.\n- **Seams:** `store_error_response` and `invariant_error` match the brief exactly.\n- **Motivating incident (the ghost row):** base answers 404 `{\"error\":\"not found\"}`. Head answers 500 `{\"error\":\"internal error\"}` with exactly one `level=error msg=invariant_violated` line.\n\n## BLOCKING\n\n### B1 — `service/today.gleam:1153-1164` (log_drill's first lookup)\n**What I saw.**\n- I set up progress so the real composer offered `tq-country-boom-chick-ladder` in an open Full sitting.\n- `PUT /api/packs/country {\"enabled\":false}` returned 200.\n- Then `POST /api/sessions/2/drills {\"drill_id\":\"tq-country-boom-chick-ladder\",\"outcome\":\"done\"}`:\n  - head: 500 `{\"error\":\"internal error\"}` and `level=error msg=invariant_violated detail=\"session_drills row names drill \\\"tq-country-boom-chick-ladder\\\" absent from the compiled content\"`\n  - base: 404 `{\"error\":\"not found\"}`, no log line.\n- After re-enabling the pack, the same POST returns 201.\n- `GET /api/sessions/open` while the pack is disabled returns 409 `session could not be resumed` on both base and head.\n- `packs_service.raw_curriculum` finds the drill with provenance `Pack(\"country\")`.\n- Probe: a scratch module calling `router.handle_request` through `test_services.with_dev`.\n\n**Likely cause (inferred from source).** The lookup runs over `today.curriculum(owner, premium_entitled)`, which is `packs.active_curriculum_in_conn`. That function filters out disabled packs (packs.gleam:274-289, 345-354). So a user toggle the user can undo triggers a 5xx and a false invariant alarm, and the detail string is untrue.\n\n**Required fix.** Raise `Invariant` only when the drill is missing from the compiled, unfiltered content. A drill hidden by a disabled pack needs its own named variant that does not log an invariant. The architect picks its status, for example the resume path's 409, or 422. These lines are inside E1's blast radius (1142-1149).\n\n### B2 — `service/onboarding.gleam:21-23, 57-72` → `api/onboarding.gleam:26` → `handlers.gleam:71-74`\n**What I saw.**\n- I deleted the seeded onboarding row through `fixture.owner` (`delete from onboarding`).\n- `GET /api/onboarding` and `PUT /api/onboarding`: head 404 `{\"error\":\"not found\"}` with no log; base 500 `{\"error\":\"internal error\"}`.\n- Mutant M1 (`store_error_response`: `NotFound -> internal_error()`) leaves all 7 api modules green (20/20/5/18/18/46/7). No test depends on that arm.\n\n**Likely cause.**\n- The onboarding store says `NotFound` happens \"only for a database that never ran the seeding migration\" (store/onboarding.gleam:22-23). That is a server-side miss with no client id.\n- The onboarding service passes it through unchanged as `Store(NotFound)`, and the new shared mapping turns that into a 404.\n- The diff moved five services' `Store(NotFound)` from 500 to 404: curriculum, drill_mastery, library, onboarding and packs. In those five, this singleton is the only reachable source. Every other `NotFound` there comes from an INSERT/UPSERT…RETURNING, which cannot return an empty result.\n- So on every reachable path, the new 404 arm turns a server invariant into a client 404. That contradicts E1's property and the handlers.gleam:34-41 comment (\"never masquerades as a client 404\").\n\n**Required fix.** Classify the singleton miss where it happens: an onboarding `Invariant(detail)` routed to `handlers.invariant_error`. The alternative is for the architect to record a narrower E1 on 1bk.6.1. The file is owned but outside E1's named radius, so the architect has to extend the radius.\n\n### B3 — `service/assess.gleam:884-887` (already on base; E1 property violated)\n**What I saw.**\n- `GET /api/assessments/checkpoint?kind=gate&module_id=th-major-scale-spelling`.\n- Then `update assessments set module_id='ghost-gate-module' where id=<cid>` to simulate content drift.\n- Then `POST /api/assessments/checkpoint` with every answer correct: 404 `{\"error\":\"module not found\"}`, no log, on both head and base.\n- Control run without the update: 200 `passed:true`.\n\n**Likely cause.** The module id comes from the stored gate header (assess.gleam:875-876). The request itself names only `checkpoint_id`. This is the same server-derived shape as the drill id in today.gleam, which only passes after the `is_member` check, and the slice reclassified that one. The implementer's diy design note quotes these same lines. E1 says: \"for every /api/* route … a lookup miss on an id the server derived answers 500\".\n\n**Required fix.** Reclassify this miss as `Invariant`, either through `wrap_today` or with an assess arm. It is outside E1's named radius, so the architect has to extend the radius or narrow E1 on the bead.\n\n## Nits\n- **N1.** A store miss wrapped inside `Today` (`Today(Store(NotFound))`) answers 404 in curriculum, library and review, but 500 in assess and drills. In curriculum, `Library(Today(Store(NotFound)))` answers 500. This contradicts the handlers.gleam:64-69 claim of one answer \"on every route\". Nothing produces these errors at runtime today: the packs store behind today.gleam:228 never returns `NotFound`, and the progression writes behind :1244 are upserts.\n- **N2.** The 1bk.6.1 design note and the reply table say review.gleam:559-562 takes `demotion.module_id` from \"the persisted rewind proposal\". It is computed during the request (review.gleam:526; rewind.gleam:59 `module_id: candidate.module.id`). The classification is still right, and the arm cannot be reached.\n- **N3.** Two things have no test: the \"one `invariant_violated` line\" part of E1 (mutant M2, which drops `log.error`, leaves api@today_test at 46 green; the brief accepts quoting stdout instead), and the `NotFound→404` arm of `store_error_response` (M1).\n- **N4.** The brief's line numbers for the preserved assertions (for example library_test:136, today_test:1078) don't match 9908cfa. I matched the assertions by test name instead.\n- **N5.** The `from_packs_error` detail text names `active_curriculum` even when the error comes from `enabled_lane_order_in_conn` (today.gleam:1438). Those arms cannot be reached.\n\n## Declared excursions (none blocking; all in owned files)\n1. **today.gleam:1558**, `finish_sitting_inline`'s empty-result arm now returns `SessionNotFound`. E1's zero-mints grep requires it. The arm can't be reached because the same transaction already read the row at 1391. I forced a miss with a mutant: base 404 `not found`, head 404 `session not found`. The status is unchanged.\n2. **Import changes.** Removals: api/assess:27, api/curriculum:53, api/packs:20, service/curriculum:31. Additions: api/drills:11, handlers:21,24. `--warnings-as-errors` forces them: putting `store_types` back into api/packs fails the build with \"Unused imported module\".\n3. **progression.gleam:145-182** is re-indented because of E2's new early-`Ok` arm. No reachable path changes: all four callers pass module ids taken from content.\n4. **library.gleam:163, 170-172** are doc comment changes only. The `drill_mastery.gleam:37` variant insert is also required: at base the type spans lines 28-38, not the radius's 28-34.\n\n## SCOPE\n- **S1.** Deleting other seeded singleton rows gives 404 on base and head alike, through the rewriting wrappers the brief keeps (today.gleam:80, review.gleam:55):\n  - missing `settings` row: `GET /api/settings` answers 404 `not found`\n  - missing `settings`, `rotation` or `review_state` rows: `GET /api/today`, `POST /api/sessions` and `GET /api/review` answer 404 `session not found`\n  \n  These are client-style 404s for server-side misses. The architect should rule on them together with B2.\n- **S2.** The `api/curriculum.gleam:79-80` arm for `Library(Today(Invariant))` cannot be reached: curriculum only calls library through `checks_for_items_in_conn`, which only ever returns `Library(Store(_))`. The \"own or wrapped\" clause requires the arm.\n\nIsolation: git clones under `/tmp/1bk1-errors-A`. Dependency caches were copied from the worktree with first-party artefacts purged. `TMPDIR` pointed inside the scratch dir, and every command ran in the nix dev shell. I deleted the scratch dir, created no worktrees, and the reviewed worktree is clean at 7037166. I made no bd writes and did not contact the implementer.\n\nCoverage: 12 families, 59 probes, 3 skipped — local://oracle-errors-A.md\n\nModel: anthropic/claude-opus-5-5 (the model in the harness workstation block; harness role @ORACLE)\n\nVERDICT: REJECT",
  "blocking": [
    {
      "id": "B1",
      "location": "server/src/woodshed_server/service/today.gleam:1153-1164",
      "finding": "A pack drill offered by the composer, with its pack then disabled: head 500 plus invariant_violated with a false 'absent from the compiled content' detail; base 404. Re-enabling the pack gives 201.",
      "fix": "Raise Invariant only when the drill is missing from the compiled content. A pack-filtered drill gets a named variant with no invariant log (architect picks the status)."
    },
    {
      "id": "B2",
      "location": "server/src/woodshed_server/service/onboarding.gleam:21-23,57-72; api/onboarding.gleam:26; handlers.gleam:71-74",
      "finding": "Onboarding singleton missing: GET/PUT /api/onboarding answer 404 on head, 500 on base. No test depends on the NotFound arm (M1 survives).",
      "fix": "Classify the singleton miss as Invariant in the onboarding service, or the architect narrows E1 (radius extension needed)."
    },
    {
      "id": "B3",
      "location": "server/src/woodshed_server/service/assess.gleam:884-887",
      "finding": "Gate header module id missing from content: POST /api/assessments/checkpoint answers 404 'module not found' on head and base. The E1 property says 500 for every /api route.",
      "fix": "Reclassify as Invariant; the architect extends the radius or narrows E1."
    }
  ]
}