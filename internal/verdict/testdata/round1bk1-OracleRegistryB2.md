{
  "verdict": "REJECT",
  "coverage": "18 families, 63 probes, 0 skipped — local://oracle-registry-B-r1.md",
  "hash": "a313609",
  "model": "anthropic/claude-opus-5-5",
  "report": "# Seat B re-review of slice/1bk1-registry at a313609 (fix round 1, from 706e33f): REJECT\n\nThe two product blockers from the last round (B1, B2) are fixed. The verdict is REJECT because of one new blocker, D1: three public doc comments contradict what the code does. Fixing them is a doc-only edit inside the fix list's docs radius.\n\n## What the fix round got right\n\n- **B1 is fixed (delete arriving while the tenant is provisioning).** P1 at head: resolve1 got `Error(Evicted)` and resolve2 (sent after the delete) got `Error(Evicted)`. The opened owner was already dead while retire was still held. resolve3 got `Error(Deleting)` and the delete returned `Ok(Ok(Nil))`. The gen-0 main, -wal and -shm files are all gone, and the next resolve lands on `dp.g1.db`. On 706e33f the same probe hands both resolves a live owner, so the probe tells the two builds apart.\n- **B2 is fixed (late provisioning result landing on a delete).** P2 at head: the late owner stopped within 2s while the tenant was Deleting, and was still dead 300ms after the delete. On 706e33f it was still alive after the delete.\n- **Replay hit counts.** Uncached start, 200 trials: head 0/200, 706e33f 27/200. Cached start: 0/200 on both. The no-evict stress run (100 iterations): head 0 hits, 706e33f 100 hits.\n- **Mutant legs all behave as the fix list requires.**\n\n  | Criterion | Mutant | Test removed | Test present |\n  |---|---|---|---|\n  | A2' | Seat A's early reply (delete on a provisioning tenant replies at once) | 30/30 green | only the A2' test red |\n  | A2' | 706e33f tree with the a313609 test file | 29/29 green | both new tests red |\n  | A2' | Only the new `unwanted` check reverted to 706e33f's shape | 30/30 green | only the A2' test red |\n  | A5 | `close_leaked_owner` removed from the Deleting arm | 30/30 green | only the A5 test red |\n\n  The unmutated leg is 31/31 green. Both new tests passed in 25 of 25 repeat runs.\n- **Last round's legs still hold.** A1, A3 and A4 behave as before; A2 is now also caught by the new A2' test.\n- **The tests named in the preserve clause are green**, including `evict_during_in_flight_provisioning_closes_connection_without_leaking_test`, the two-evictor tests, `health_resets_on_a_successful_provisioning_test` and A3/A4.\n- **F3.** `Retired(previous_generation: Int)` has exactly one field. `grep -c previous_generation` gives 3 in src and 5 in the test file. No sibling slice constructs `Retired`.\n- **Parked-delete order.** P11 at head runs the retire from the first request (`\"first\"`); 706e33f ran `\"third\"`.\n- **Unparseable ids.** At head, seven bad ids each return `RetireFailed` with 0 retire calls and the generation left at 0. On 706e33f the same ids made 7 retire calls and moved the generation to 7. The 128-character boundary id is still accepted.\n- **Permission restore.** Under the A4 mutant, the head test run leaves 0 read-only directories behind; 706e33f leaves 1.\n- **Settle paths.** A parked delete combined with an open error, a crash, a timeout, a failed retire plus a parked evict, and a second tenant: every case matches design record part 1.\n- **Late results are stopped in all four slot states** (empty, Ready, a newer attempt, Deleting).\n- **The implementer's claims check out by measurement.** The md5 sums, numstat and 61 deleted lines match. Every deleted line falls inside the fix list's blast radius.\n- **Full suite** (run once, the way CI runs it): exit 0, `678 passed, no failures`.\n\n## BLOCKING\n\n**D1. Three public doc comments say something the code does not do.** Locations in `server/src/woodshed_server/tenant_registry.gleam`:\n- `:483-486`, the `delete_tenant` doc: \"Resolves arriving for the whole window are refused `Error(Deleting)` immediately\".\n- `:222-225`, the `ResolveError.Deleting` doc: \"resolves are REFUSED for the whole delete window -- never parked\".\n- `:43-45`, the module doc: \"refuses every resolve outright ... never parks\".\n\nThis diff defines the window as opening when `delete_tenant` is called (module doc `:50-54`, State doc `:337-340`, design record part 1). Under that definition the three sentences above are false.\n\n*Finding.* Probe P_DOC, run at head with TMPDIR under the scratch dir:\n1. Start a gated provisioning attempt.\n2. Call `delete_tenant`.\n3. Send resolve2 and keep the gate held for 1000ms. Result: `resolve2 answered within 1000ms of the call (gate held): \"NO REPLY\"`.\n4. Release the gate. Result: `resolve2 after the open settled: \"Error(Evicted)\"`.\n\nP1 shows the same thing: resolve2, sent after the delete, gets `Error(Evicted)`.\n\n*Explanation (from reading the code, not probed separately).* `handle_resolve` (`:562-576`) parks any resolve on a Provisioning slot regardless of `parked_deletes`. Only the DeletingTenant arm (`:577-585`) refuses immediately. The implementer's reply lists this as \"deliberately left alone\", but the docs were not updated to match.\n\n*Why it matters.* The later W1-D handler slice will map `ResolveError` using these docs. In the parked phase a request actually blocks until the open settles, which can take up to `provision_timeout_ms`, and then gets `Evicted` rather than `Deleting`.\n\n*Required fix (either one).*\n- (a) Amend the three doc sites to say: while a delete is parked on an in-flight attempt, a resolve parks on that attempt and is answered `Evicted`, or the attempt's own error, when it settles. Only the installed Deleting state refuses immediately. This stays within the fix list's docs radius.\n- (b) Make `handle_resolve` answer `Error(Deleting)` immediately when `parked_deletes` holds the tenant, which makes the current docs true.\n\nA2' passes either way.\n\n## Nits (do not gate)\n\n- **N1. No test covers the two nit fixes.** Reverting to prepend order leaves the suite at `All 31 tests passed.`; so does removing the id parse in `delete_tenant`. The fix list did not ask for tests.\n- **N2. `RetireFailed` now also means \"unparseable id\".** That is a caller fault inside a server-fault variant, against the PER-6 split this module draws for `ResolveError` (`:191-204`). The arbiter ordered this shape, and the path cannot be reached over HTTP.\n- **N3. Design record part 3 has stale line numbers.** It cites web.gleam resolve sites 705/771/792. At a313609, on master and in every sibling worktree they are at 689/759/780.\n- **N4. The `Evicted` doc (`:217-221`) names only `evict`.** A delete parked on an attempt now also produces `Evicted`.\n- **N5. `parked_deletes` lives on State but must stay in step with the Provisioning slot.** This diff adds a second reader, `unwanted` at `:814-817`. The invariant does hold: every exit from Provisioning goes through `continue_after_settle` (`:832`, `:842`, `:879`, `:883`, `:954`, `:1356`). A `parked_deletes` field on `Provisioning`, next to `pending_evicts`, would make `unwanted` a local check and delete the unreachable `Ok([])` arm (`:1261-1270`). The structure predates this diff.\n- **N6 (carried).** The A3 test asserts at tenant_registry_test.gleam `:1541` before restoring permissions at `:1542`. This is the same pattern the tenant_db_test nit fixed. It is visible in the code but I could not reproduce it: the A3 mutant fails at an earlier assertion.\n- **N7 (carried).** The \"Shouldn't happen\" comment on the `Error(Nil)` arm (`:767-772`) is stale: the late-result-on-empty-slot probe exercises that arm.\n- **N8 (carried).** There is still no deadline on a wedged retire. The arbiter ruled one is not required.\n\n## SCOPE (does not gate)\n\n- **S1. The parse-first guard in `delete_tenant` protects a path no caller reaches.** This covers the guard itself (`:501-506`), its `RetireFailed` doc sentence, and the named unreachable `RemoveInvalidTenantId` arm (`:1171-1173`). There are 0 callers of `delete_tenant` in src at a313609 or in any sibling slice. The planned W1-D caller passes `ctx.tenant_id`, which has already passed `tenant_registry.resolve` at web.gleam:689/759/780. The guard need not exist: a doc fix alone would have made the `RetireFailed` doc true. The arbiter ordered it; recorded here for the orchestrator only.\n\n## Model and cleanup\n\nRan as anthropic/claude-opus-5-5. Scratch `/tmp/1bk1-registry-B-r1` is deleted. The worktree is clean at a313609 and the worktree list is unchanged (5 entries).\n\nCoverage: 18 families, 63 probes, 0 skipped — local://oracle-registry-B-r1.md\nVERDICT: REJECT (binds a313609)"
}